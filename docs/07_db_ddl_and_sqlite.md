# MVP1 DB Deliverables (DDL + SQLite/PG)

This bundle adds missing DB assets:
- Reference DDL for Postgres and SQLite (review purpose)
- A concrete Alembic initial migration to create MVP1 core tables (source of truth)
- SQLite PoC guidance and Postgres prod guidance

**Source of truth:** Alembic migrations in `backend/alembic/versions/`.
