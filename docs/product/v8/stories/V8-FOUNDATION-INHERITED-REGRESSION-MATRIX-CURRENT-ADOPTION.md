# Story V8-FOUNDATION-INHERITED-REGRESSION-MATRIX-CURRENT-ADOPTION

- Risk: `PROTECTED`
- Candidate commits: `527f5179a5145d0749d659f005bc13f95097afa2`,
  `a0deb51b6556c881749dc2e7a6b0b206dafd6e5d`,
  `7439bcd18f13fb61887b784d39436147afc23a4b`
- Catalog ID: `FPMS-V8-FOUNDATION-INHERITED-REGRESSION-MATRIX-20260712-01`
  (ordinal `279`, Foundation).
- Outcome: bind every one of the 70 inherited Additional-Gap tasks exactly once to
  current reviewed successor authority and named verification paths, then verify the
  current V8 backend, inherited UI and real-stack overlay behavior without reviving the
  retired taskctl control plane or obsolete product assumptions.

## Exact closure and authority

The tracked contract at
`backend/tests/test_v8_foundation_inherited_regression_matrix_contract.py` freezes the 70
inherited task identities exactly once. A still-current inherited task binds directly to
its own tracked task contract and the test/spec paths declared by that contract. Only a
task whose authority was genuinely replaced binds to named `CURRENT_VERIFIED` successor
stories. The contract proves task uniqueness, task-file reachability, exact authority
kind, verification-path reachability and, for successors, current status plus independent
review references. Tasks 01 and 05–12 have additional literal assertions so their distinct
wizard, resolve-key, filing ensure/resolve, OA ensure/resolve and UI paths cannot collapse
back into a coarse package bundle.

The two inherited control-plane tasks for the taskctl manifest release gate and old final
close ledger, plus their historical evidence-sanitation prerequisite, map only to
`C3-LEAN-GOVERNANCE-ADOPTION`. Their four historical test modules remain collected but are
explicitly skipped with the C3 supersession reason. No product, legal, fee, deadline,
lineage, permission, schema or migration rule is skipped.

The historical Task46 seven-GAP test is not a current executable contract: it creates an OA
document and expects that generic document creation changes the case to `OA1`. The reviewed
V8 successor makes generic document creation lifecycle-neutral and requires the evidence
adapter for legal-state changes. Restoring the old behavior would violate the current
fail-closed lifecycle contract. Task46 therefore resolves to the current seven-domain
successor bundle and the real Vite/FastAPI/SQLite three-lane overlay E2E rather than to that
obsolete auto-status assertion. Its exact current authority is
`V8-LIFECYCLE-OVERLAY-REAL-UI-E2E-CURRENT-ADOPTION`, alongside the reviewed seven-domain
successor bundle, and its executable path is `v8-lifecycle-overlay-live.spec.ts`.

## Successor compatibility corrections

The current matrix required only test-harness corrections:

- cumulative development seeding now expects the already adopted row-129 notice activation
  while the row-128 focused seeder retains its own exact predecessor expectation;
- case-create evidence fixtures provide the required explicit neutral fee-reduction value;
- document-detail bodylessness is proved both at the mounted `/api/v1` OpenAPI operation and
  the local handler;
- schema tests prove their frozen revisions remain reachable from the unique current
  `v8_d31_overlay_conflict_01` head and include the exact conflict-lineage successor columns;
- lifecycle lazy-import verification is test-order independent;
- the shared SQLite test fixture restores `PRAGMA foreign_keys=ON` on every pool checkout.

No product source changed and no assertion was weakened to accept invalid legal, fee,
deadline, lineage, permission or schema behavior.

## Fresh verification

- Current backend V8 matrix: `4643 passed, 24 skipped, 114 subtests passed` in 1259.16s.
  The 24 skips are only the four explicitly superseded control-plane modules.
- Exact Row279/route correction: `15 passed`; final authority-contract correction:
  `5 passed`; scoped Ruff, format and diff checks passed.
- Inherited UI matrix excluding the obsolete Task46 scenario: `22 passed` with one worker.
- Current real-stack overlay E2E on isolated SQLite, standard seed, dedicated FastAPI and
  Vite: `1 passed` with real login and no fulfilled application routes.
- The first independent review found two P1 evidence-quality defects. The correction binds
  exact per-task authorities/paths and restores application-level route proof; final
  independent review is recorded in the companion review.

The exact final candidate tree fingerprint for the fifteen test paths is
`5b056e16e92fe5ed7e6a33cc4b719cf404a9dde57a7e2e0863887dc2d34cbefc`.

## Non-goals and rollback

This story does not repair or reinterpret product behavior, reactivate old taskctl,
materialize 70 implementation tasks, mutate customer decisions, or run the final release
gate. Rollback reverts the three candidate commits and this adoption record; it does not
change any previously accepted V8 product story.
