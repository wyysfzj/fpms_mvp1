# Story V8-OVERLAY-FE-ADAPTER-CURRENT-CONTRACT

- Risk: `NORMAL`
- Outcome: close catalog row `266` with a lossless typed frontend transport for the accepted
  lifecycle-overlay HTTP response.
- Catalog ID: `FPMS-V8-OVERLAY-FE-ADAPTER-20260712-01`.
- Dependency: begin only after the row-265 HTTP adapter is independently accepted.

## Exact closure

Create the dedicated public camelCase overlay types, the Axios adapter and one compile-only
contract probe. Preserve all frozen unions, fifteen nested DTOs, required nullable fields and
readonly arrays. Dates/timestamps remain strings; official/source/payable amounts and reduction
ratios remain decimal strings. Keep document derivations, package receipts, fee lines/related
facts, milestone warnings and top-level warnings in their separate nested positions.

The private snake_case wire shape maps recursively and explicitly to public DTOs. Send
`after_sequence`, `limit` and `as_of_revision`; Axios omission of null yields the backend's
first-page null semantics. Return direct response data after mapping and do not catch, wrap,
retry, translate or reconstruct backend errors.

Map decision gates in response order with no sort/filter/index/deduplication. Export the composite
consumer key `(gateCode, requestedScopeKey)` so 29 entries remain distinct despite only eight
codes. Preserve individual requested form scope, possible resolved `ALL-22`, decision value and
source provenance exactly on every page.

## Verification and non-goals

The compile-only probe proves exact unions/shapes/nullability, nested associations, decimal
strings, API signatures, first/intermediate/final/empty pages, 29 ordered composite identities and
rejection of code-indexed, numeric-money, flattened or first-page-only alternatives. Run the
frontend typecheck once under serialized verification, scoped ESLint/diff, then independent
per-wave High review.

No UI, cache, gate resolver, endpoint policy, numeric conversion, `Date`, schema generator,
shared-type refactor or adjacent API cleanup. Rollback removes only the three row-266 files and
its adoption.
