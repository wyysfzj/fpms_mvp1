# TODO / Open Decisions

## Data & business
1. CaseNo numbering rule: global sequence vs per-case-type vs per-year.
2. Applicant modeling: enforce master data now or allow free-text and migrate later.
3. Document classification taxonomy: minimal set for MVP1.
4. Task template set: which task templates are mandatory in MVP1.
5. Fee code & fee rate strategy: initial seed list + who maintains it.
6. Multi-currency handling: MVP1 supports currency field but no FX conversion unless needed.
7. Attachments storage policy: allowed file types, max size, retention.

## Security
1. Password policy & reset flow (email vs admin reset).
2. Audit log: which operations must be auditable in MVP1.

## Office templates
1. Template file repository governance (naming, versioning).
2. Required outputs for MVP1 (Bill CN/EN, Task sheet, Document list).

## UX
1. Default list filters per role (“My tasks”, “My cases”).
2. Bulk operations priority: batch filing, batch task assignment, batch bill generation.

