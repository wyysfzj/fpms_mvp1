# Document APIs (MVP1)

Base: `/api/v1/documents`

- GET /documents (search)
- POST /documents (register)
- GET /documents/{id}
- PUT /documents/{id}
- POST /documents/{id}/attachments (multipart upload)
- GET /documents/{id}/attachments/{att_id}/download

