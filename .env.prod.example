# Production example (docker-compose.prod.yml overrides DATABASE_URL)
FPMS_ENV=prod
# For reverse proxy deployments, adjust this to the real origin(s)
CORS_ORIGINS=["https://fpms.example.com"]
JWT_SECRET=change-me
