# 授权费草单联动烟雾说明

## 覆盖范围

- 页面：`/grant-fee/tasks`
- 触发方式：任务行内 `生成草单`
- 前提条件：任务状态为 `READY_TO_DRAFT` 且尚未生成草单
- 结果反馈：成功后刷新列表，并让行状态显示为 `DRAFT_GENERATED`

## 验收要点

- 只保留最小单行触发，不提供复杂批量选择器
- 不弹出结果模态窗平台
- 不提供重试 UI
- 不涉及账单或文书联动

## 备注

- 页面文案保持简体中文
- 仅验证前端最小草单触发入口是否可用
