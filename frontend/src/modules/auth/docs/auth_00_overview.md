# Frontend Auth (MVP1)

## Responsibilities
- Provide login form to call POST `/auth/login`
- Store token in localStorage (MVP1)
- Load `/auth/me` after login to obtain permissions
- Route guard & menu rendering by permissions (future improvement)

