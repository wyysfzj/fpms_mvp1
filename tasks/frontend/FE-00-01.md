# FE-00-01 — App layout + router shell
> Prerequisite: backend is running with DB migrated.  
> Reference: `backend/docs/db_migrations_overview.md`
## Design references
- `docs/01_information_architecture.md`
- `docs/05_frontend_architecture.md`

## Target
- **File:** `frontend/src/layout/MainLayout.vue`
- **Atomic rule:** modify/create ONLY this file.

## Scope decision (MVP1 – FIXED)
- Scope is FIXED for MVP1.
- Implement ONLY what is explicitly listed below.
- Do NOT add additional routes/components/stores/APIs beyond this file.

## Components to implement (EXACT)
### MainLayout.vue
Implement a minimal application shell with:
- Left sidebar navigation (router-links) to:
  - /dashboard
  - /cases
  - /documents
  - /tasks
  - /fees/drafts
  - /billing/bills
  - /settings/clients
- A main content area that renders `<router-view/>`
- No permission-based hiding in this task (handled later).

## Non-scope (explicitly excluded)
- No router changes
- No auth guard
- No API calls
- No Pinia stores
- No Element Plus components required (plain HTML is acceptable)

## Prompt
Implement `MainLayout.vue` EXACTLY per the components list above, following the information architecture in the referenced docs.

Do NOT ask clarification questions.
Do NOT add optional features.

## Acceptance checklist
- [ ] Only frontend/src/layout/MainLayout.vue was changed/created.
- [ ] App loads without console errors.
- [ ] Navigation links render.

## Validation commands
### Frontend
```bash
cd frontend
npm run build
npm run dev  # manual UI check
```

Or run helper:
```bash
./scripts/run_frontend_checks.sh
```
