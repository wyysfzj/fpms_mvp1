# BE-00-01 — FastAPI app skeleton

## Design references
- `docs/04_backend_architecture.md`
- `docs/06_deployment.md`
  
## Target
- **File:** `backend/app/main.py`
- **Atomic rule:** modify/create ONLY this file; implement ONLY what is described below.

## Scope decision (MVP1 – FIXED)
- This task ONLY wires the FastAPI application.
- No business logic, no models, no APIs beyond health check.

## Components to implement (EXACT)
- FastAPI app factory
- CORS middleware
- API router mounted at `/api/v1`
- Health endpoint `/healthz`
  
## Non-scope (explicitly excluded)
- Database models
- Alembic migrations
- Auth logic
- Any domain APIs
  
 ## Prompt
Implement FastAPI bootstrap EXACTLY as specified.

Do NOT add additional routes or logic.
Do NOT ask clarification questions.

## Acceptance checklist
- [ ] Only backend/app/main.py was changed/created.
- [ ] App starts with uvicorn without errors.
- [ ] CORS and router inclusion are present.
- [ ] Health endpoint responds 200.

## Validation commands
### Backend
```bash
cd backend
ruff check .
python -m py_compile app/main.py
pytest -q  # optional
```

Or run helper:
```bash
./scripts/run_backend_file_checks.sh app/main.py
```

Optional smoke test:
```bash
curl -s http://localhost:8000/healthz
```
