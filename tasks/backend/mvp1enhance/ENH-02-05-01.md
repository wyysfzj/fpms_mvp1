
# ENH-02-05-01 — Fix `CaseCreateIn` Schema in `schemas.py`

## Context / Why
The `POST /api/v1/cases` endpoint is failing with an **ImportError**: `cannot import name 'CaseCreateIn'`. This is due to the missing or incorrectly defined `CaseCreateIn` schema in `app/modules/cases/schemas.py`.

This task will fix the ImportError by ensuring that the **`CaseCreateIn`** schema is correctly defined and available for import in `api.py`.

## Target (Atomic – FIXED)
Define or restore the **`CaseCreateIn`** schema in `app/modules/cases/schemas.py` to be used in the `POST /api/v1/cases` endpoint.

## Allowed Files (Strict Allowlist)
- `backend/app/modules/cases/schemas.py`

## Non-scope (explicitly excluded)
- Do **NOT** modify any other files outside `schemas.py`.
- Do **NOT** modify any other schemas unless directly related to the `CaseCreateIn` schema.

## Required Change (EXACT)
1) In `schemas.py`, ensure that the `CaseCreateIn` schema is defined correctly:
   - Define the required fields for the case creation process.
   - Ensure it uses **Pydantic** for validation.
   
2) Ensure that `CaseCreateIn` is imported correctly in `api.py` to avoid the `ImportError`.

## Acceptance checklist
- [ ] `CaseCreateIn` schema is defined correctly in `schemas.py`.
- [ ] `CaseCreateIn` is correctly imported in `api.py`.
- [ ] The `ImportError` for `CaseCreateIn` is resolved.
- [ ] Code passes `ruff check` and `py_compile` for `schemas.py`.

## Evidence
Provide:
- Command outputs from running `ruff check`, `ruff format`, and `python3 -m py_compile` for `schemas.py`.
- `git diff` showing only the changes in `schemas.py`.
- Ensure that `CaseCreateIn` can be imported without errors in `api.py`.

## Validation commands
```bash
cd backend
ruff check app/modules/cases/schemas.py
ruff format app/modules/cases/schemas.py
python3 -m py_compile app/modules/cases/schemas.py
cd ..
```
