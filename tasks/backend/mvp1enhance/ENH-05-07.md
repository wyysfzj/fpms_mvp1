# ENH-05-07 — GET /tasks/{id} (get task detail)

## Context
Implement Tasks API endpoint per API spec and review report §5.5. This endpoint must delegate business logic to `tasks/service.py` and use Pydantic schemas from `tasks/schemas.py`.

## Target (Atomic)
- **File:** `backend/app/modules/tasks/api.py`

## Allowlist
- `backend/app/modules/tasks/api.py` ONLY

## Non-scope
- Do NOT modify service.py or schemas.py
- Do NOT change DB/migrations
- Do NOT add unrelated endpoints

## Endpoint (EXACT)
- Method: `GET`
- Path: `/tasks/{id}`
- Permission: `Task.Read` via `require_perm("Task.Read")`
- Success status: `200`

## Expected status codes (realistic)
- 401: missing/invalid token
- 403: token OK but missing `Task.Read`
- 422: token+perm OK but request validation fails (where applicable)
- 200: success (token+perm OK and request valid)

## Required implementation (EXACT)
- Implement ONLY `GET /tasks/{id}`.
- Path param: `id`
- Call `get_task(db, task_id=id)`
- Return `TaskOut`

## Validation commands
```bash
cd backend
ruff check app/modules/tasks/api.py
python3 -m py_compile app/modules/tasks/api.py
```

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
export TASK_ID="REPLACE_ID"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/tasks/$TASK_ID"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-05-07**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Do NOT ask clarification questions; use referenced design docs in-repo as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-05-07 lint bash -lc "cd backend && ruff check app/modules/tasks/api.py"
./scripts/evidence_run.sh ENH-05-07 fmt  bash -lc "cd backend && ruff format app/modules/tasks/api.py"
./scripts/evidence_run.sh ENH-05-07 test bash -lc "cd backend && python3 -m py_compile app/modules/tasks/api.py"
./scripts/evidence_finalize.sh ENH-05-07
./scripts/task_validate.sh ENH-05-07
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- The referenced design docs contradict this task in a way that requires expanding scope
