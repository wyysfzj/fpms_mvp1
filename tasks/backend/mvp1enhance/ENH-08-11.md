# ENH-08-11 — LetterHeads — Create (POST /letterheads)

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/templates/api.py`

## Allowlist
- `backend/app/modules/templates/api.py` ONLY

## Endpoint (EXACT)
- Method: POST
- Path: /letterheads
- Permission: `LetterHead.Create` via `require_perm("LetterHead.Create")`
- Success status: 201

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `LetterHead.Create`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 201: success

## Required implementation (EXACT)
- Implement ONLY `POST /letterheads`.
- Body schema: `LetterHeadCreateIn`
- Call `create_letterhead(db, data=payload, actor_id=current_user.id)`
- Return `LetterHeadOut`
- Ensure HTTP 201.

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"Default LetterHead","locale":"zh-CN","is_default":true}' \
  "http://localhost:8000/api/v1/letterheads"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-11**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-11 lint bash -lc "cd backend && ruff check app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-11 fmt  bash -lc "cd backend && ruff format app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-11 test bash -lc "cd backend && python3 -m py_compile app/modules/templates/api.py"
./scripts/evidence_finalize.sh ENH-08-11
./scripts/task_validate.sh ENH-08-11
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
