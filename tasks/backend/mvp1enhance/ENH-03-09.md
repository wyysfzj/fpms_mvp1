# ENH-03-09 — Billing Print: BillContextBuilder (dict context) (BL-DOC-02)

## Context / Why
The Billing print feature needs a deterministic, DB-free context builder to produce a JSON-serializable dict for docxtpl rendering.

This implements the required builder described in BL-DOC-02.

## Design references (authoritative)
- `tasks/backend/business_logic/doc_render/BL-DOC-02_billing_bill_context_builder.md`
- `docs/04_backend_architecture.md` (template context builder pattern)
- `docs/00_mvp1_scope.md`

If any reference conflicts, BL-DOC-02 wins.

## Target (Atomic – FIXED)
Create exactly one new file:
- `backend/app/modules/billing/doc_render_bill_context.py`

And implement:

```python
class BillContextBuilder:
    def build(self, bill, bill_items, client, letter_head) -> dict:
        ...
```

## Allowed files (Strict allowlist)
- `backend/app/modules/billing/doc_render_bill_context.py` ONLY (create new file)

## Non-scope (explicitly excluded)
- No DB queries inside the builder
- No API changes
- No template path logic (handled in ENH-03-10)

## Required change (EXACT)
- Implement `BillContextBuilder.build(...)` returning a pure-JSON-serializable dict with at least keys:
  - `bill` (dict)
  - `client` (dict)
  - `items` (list[dict])
  - `letter_head` (dict or None)
- Use ORM field names (inspect existing models; do not guess names)

## Acceptance checklist
- [ ] Only the new file is created/changed
- [ ] Import succeeds:
  `PYTHONPATH=backend python -c "from app.modules.billing.doc_render_bill_context import BillContextBuilder; print('OK')"`
- [ ] `build(...)` returns a dict containing keys: `bill`, `client`, `items`, `letter_head`
- [ ] Ruff + py_compile pass; task gate passes

## Evidence (mandatory)
- `artifacts/ENH-03-09/git/diff.patch`
- Lint/fmt/py_compile logs
- Import proof command output


## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **one and only one** atomic task.

**Role**: bounded executor (no design, no extra improvements, no scope expansion).

You MUST:
- Implement exactly what this task requires
- Make the smallest change that satisfies Acceptance
- Collect objective evidence using EOS scripts
- STOP immediately if required changes exceed the allowlist

You MUST NOT:
- Add “nice-to-have” validations/logging/refactors
- Propose follow-up actions
- Touch files outside the allowlist


## Hard Rules (Must Follow)
- Execute ONLY ENH-03-09
- Create/modify ONLY `doc_render_bill_context.py`

## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-03-09 lint bash -lc "cd backend && ruff check app/modules/billing/doc_render_bill_context.py"
./scripts/evidence_run.sh ENH-03-09 fmt  bash -lc "cd backend && ruff format app/modules/billing/doc_render_bill_context.py"
./scripts/evidence_run.sh ENH-03-09 test bash -lc "cd backend && python3 -m py_compile app/modules/billing/doc_render_bill_context.py"
./scripts/evidence_run.sh ENH-03-09 quick bash -lc "PYTHONPATH=backend python3 -c \"from app.modules.billing.doc_render_bill_context import BillContextBuilder; print('OK')\""
./scripts/evidence_finalize.sh ENH-03-09
./scripts/task_validate.sh ENH-03-09
```

## STOP Contract (Explicit)
STOP immediately and report if:
- Any requirement cannot be satisfied without modifying files outside the allowlist
- The authoritative design docs conflict or are ambiguous for required behavior
- You are tempted to “improve” unrelated code

**Allowlist recap**:
- `backend/app/modules/billing/doc_render_bill_context.py`

