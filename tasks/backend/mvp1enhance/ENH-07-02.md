# ENH-07-02 — Clients schemas: addresses + contacts (typed, no dict)

## Context
Backend review report §5.7 item **7.4** requires addresses/contacts to use proper Pydantic schemas (not `dict`) (P1).

## Target (Atomic)
- File: `backend/app/modules/masterdata/clients/schemas.py`

## Allowlist
- `backend/app/modules/masterdata/clients/schemas.py` ONLY

## Non-scope
- No service/API changes
- No DB/migration changes

## Required implementation (EXACT)
Extend `schemas.py` to define typed schemas for client addresses and contacts.

### Rules
- Field names/types MUST match the actual ORM models / migrations in your repo:
  - `t_client_address` model and `t_client_contact` model may live in different modules; treat migration as source-of-truth.
- Do NOT use `dict[str, Any]` for addresses/contacts anywhere in schemas.

### Schemas to define
1) `ClientAddressOut` (ORM)
2) `ClientContactOut` (ORM)

3) Optional input schemas (only if the API accepts nested writes today):
   - `ClientAddressIn`
   - `ClientContactIn`

### Wiring
- If `ClientOut` currently exposes addresses/contacts, change them to:
  - `addresses: list[ClientAddressOut] | None = None`
  - `contacts: list[ClientContactOut] | None = None`

If the API does NOT expose these fields yet, you may still define the schemas but do NOT invent new endpoints.

## Acceptance checklist
- [ ] Only schemas.py changed
- [ ] No dict-typed addresses/contacts remain in schemas
- [ ] `python3 -m py_compile .../schemas.py` passes


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-07-02**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (design docs / migrations / ORM models).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-07-02 lint bash -lc "cd backend && ruff check app/modules/masterdata/clients/schemas.py"
./scripts/evidence_run.sh ENH-07-02 fmt  bash -lc "cd backend && ruff format app/modules/masterdata/clients/schemas.py"
./scripts/evidence_run.sh ENH-07-02 test bash -lc "cd backend && python3 -m py_compile app/modules/masterdata/clients/schemas.py"
./scripts/evidence_finalize.sh ENH-07-02
./scripts/task_validate.sh ENH-07-02
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required fields/nullability cannot be determined from ORM models/migrations
- The endpoint path/prefix conflicts with existing router wiring
