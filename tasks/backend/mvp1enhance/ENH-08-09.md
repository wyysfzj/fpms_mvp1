# ENH-08-09 — Templates — Update (PUT /templates/{id})

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/templates/api.py`

## Allowlist
- `backend/app/modules/templates/api.py` ONLY

## Endpoint (EXACT)
- Method: PUT
- Path: /templates/{id}
- Permission: `Template.Edit` via `require_perm("Template.Edit")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `Template.Edit`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 200: success

## Required implementation (EXACT)
- Implement ONLY `PUT /templates/{id}`.
- Path param: `id` (template_id)
- Body schema: `TemplateUpdateIn`
- Call `update_template(db, template_id=id, data=payload, actor_id=current_user.id)`
- Return `TemplateOut`

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
export TEMPLATE_ID="REPLACE_TEMPLATE_ID"
curl -i -X PUT -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"enabled":false}' \
  "http://localhost:8000/api/v1/templates/$TEMPLATE_ID"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-09**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-09 lint bash -lc "cd backend && ruff check app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-09 fmt  bash -lc "cd backend && ruff format app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-09 test bash -lc "cd backend && python3 -m py_compile app/modules/templates/api.py"
./scripts/evidence_finalize.sh ENH-08-09
./scripts/task_validate.sh ENH-08-09
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
