# ENH-07-03 — Clients service: list/get

## Context
Backend review report §5.7 item **7.2** requires a service layer (P0). API should not do direct DB writes/queries beyond delegating.

## Target (Atomic)
- File: `backend/app/modules/masterdata/clients/service.py`

## Allowlist
- `backend/app/modules/masterdata/clients/service.py` ONLY

## Non-scope
- No API changes
- No schema changes
- No DB/migration changes

## Required implementation (EXACT)
Implement these functions in `service.py` (names may vary, but must be used by API tasks later):

1) `list_clients(db, *, q: str | None, is_active: bool | None, page: int, page_size: int) -> tuple[list[Client], int]`
   - Base query on `Client` model (`t_client`)
   - Optional search `q` should match `client_code` OR `name_cn` OR `name_en` (case-insensitive if supported)
   - Optional filter `is_active`
   - Pagination and total count
   - Order by `updated_at DESC` (fallback to `created_at DESC`)

2) `get_client(db, *, client_id: str) -> Client`
   - Return client by id
   - If not found, raise project-standard NOT_FOUND BusinessError

Notes:
- Use existing project error patterns (BusinessError / http error mapper) — do not invent new frameworks.

## Acceptance checklist
- [ ] Only service.py changed
- [ ] Functions compile/import
- [ ] List returns (items, total) and supports pagination


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-07-03**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (design docs / migrations / ORM models).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-07-03 lint bash -lc "cd backend && ruff check app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-03 fmt  bash -lc "cd backend && ruff format app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-03 test bash -lc "cd backend && python3 -m py_compile app/modules/masterdata/clients/service.py"
./scripts/evidence_finalize.sh ENH-07-03
./scripts/task_validate.sh ENH-07-03
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required fields/nullability cannot be determined from ORM models/migrations
- The endpoint path/prefix conflicts with existing router wiring
