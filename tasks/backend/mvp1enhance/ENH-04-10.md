# ENH-04-10 — Documents API: GET /documents/{id} (Doc.Read)

**Source**: `tasks/backend/apis/BE-APIv4-013_documents_get_documents_id.md`  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Implement `GET /documents/{id}` endpoint using the service layer and typed response.

## Design references (authoritative)
- `backend/app/modules/documents/docs/doc_02_api.md`
- `tasks/backend/apis/BE-APIv4-013_documents_get_documents_id.md`

## Endpoint (EXACT)
- Method: `GET`
- Path: `/documents/{id}`
- Permission: `require_perm("Doc.Read")`
- Success: HTTP `200`

## Expected status codes
- 401: missing/invalid token
- 403: missing `Doc.Read`
- 404: document not found
- 200: success

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/modules/documents/api.py`.

## Allowed files (Strict allowlist)
- `backend/app/modules/documents/api.py`

## Requirements (EXACT)
In `documents/api.py`, implement ONLY this endpoint:
- `GET /documents/{id}`
- Use `require_perm("Doc.Read")`
- Call service `get_document` (ENH-04-06)
- Return `DocumentOut` response model (ENH-04-03)
- Must return 404 if doc does not exist (via service error mapping)

Hard rules:
- Do NOT add other endpoints
- Do NOT modify schemas/service/models

## Evidence required
- Ruff + py_compile logs
- Curl example:
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/documents/REPLACE_ID"
```

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-10 lint bash -lc "cd backend && ruff check app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-10 fmt  bash -lc "cd backend && ruff format app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-10 test bash -lc "cd backend && python3 -m py_compile app/modules/documents/api.py"
./scripts/evidence_finalize.sh ENH-04-10
./scripts/task_validate.sh ENH-04-10
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/modules/documents/api.py`

