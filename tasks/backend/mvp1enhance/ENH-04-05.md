# ENH-04-05 — Documents Service: create_document with case linking validation

**Source**: `mvp1_backend_review_report.md` §5.4 (Items 4.2, 4.5)  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Implement the service function that creates a document record and validates document↔case linking (case must exist).

## Design references (authoritative)
- `backend/app/modules/documents/docs/doc_02_api.md` (create API contract)
- `backend/app/modules/documents/docs/doc_01_db.md` (document fields)
- `mvp1_backend_review_report.md` §5.4 (document-case linking)

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/modules/documents/service.py`.

## Allowed files (Strict allowlist)
- `backend/app/modules/documents/service.py`

## Requirements (EXACT)
In `documents/service.py`, implement (or complete) **ONLY**:

### Function: `create_document`
Implement a function that:
- Accepts `db` session and a Pydantic payload (prefer `DocumentCreateIn`)
- Validates `case_id` exists in `t_case`
  - If case does not exist: raise a 404-equivalent error using existing project conventions
- If `doc_template_id` is provided:
  - Validate template exists in `t_doc_template` (404 if missing)
- Inserts a new `t_document` row
- Commits and refreshes the ORM instance
- Returns the created document ORM row

Hard rules:
- No attachments created here
- Do not parse `extra_data` JSON; store as-is (string)

## Acceptance checklist
- [ ] Only `documents/service.py` changed
- [ ] `create_document` exists and is importable
- [ ] Case existence is validated (404 on missing)
- [ ] Optional template existence is validated
- [ ] Ruff + py_compile pass; task gate passes

## Evidence required
- Import proof for `create_document`
- Ruff + py_compile logs

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-05 lint bash -lc "cd backend && ruff check app/modules/documents/service.py"
./scripts/evidence_run.sh ENH-04-05 fmt  bash -lc "cd backend && ruff format app/modules/documents/service.py"
./scripts/evidence_run.sh ENH-04-05 test bash -lc "cd backend && python3 -m py_compile app/modules/documents/service.py"

./scripts/evidence_run.sh ENH-04-05 quick bash -lc "PYTHONPATH=backend python3 -c "from app.modules.documents.service import create_document; print(create_document.__name__)""
./scripts/evidence_finalize.sh ENH-04-05
./scripts/task_validate.sh ENH-04-05
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/modules/documents/service.py`

