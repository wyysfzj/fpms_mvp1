# ENH-01-05 — Create auth schemas (LoginRequest, TokenResponse, MeResponse)

## Design references
- `docs/04_backend_architecture.md`
- `tasks/backend_tasks_atomic.md` (BE-01-05 to BE-01-07)

## Target
- **File:** `backend/app/modules/auth/schemas.py`
- **Atomic rule:** modify ONLY this file; create ONLY these 3 schemas.

## Prerequisites
- None (schemas are independent)

## Scope decision (MVP1 – FIXED)
- Create ONLY 3 Pydantic schemas for auth endpoints.
- This task does NOT implement the endpoints themselves.

## Components to implement (EXACT)
Replace TODO placeholder in `auth/schemas.py`:

```python
from pydantic import BaseModel, Field


class LoginRequest(BaseModel):
    """Login credentials."""
    username: str = Field(..., min_length=1, strip_whitespace=True)
    password: str = Field(..., min_length=1)


class TokenResponse(BaseModel):
    """JWT access token response."""
    access_token: str
    token_type: str = "bearer"


class MeResponse(BaseModel):
    """Current user profile with permissions."""
    user: dict  # {id, username, display_name}
    roles: list[str]  # Role codes
    permissions: list[str]  # Permission codes
```

## Non-scope (explicitly excluded)
- Implementing login endpoint
- Password strength validation
- Token refresh schemas
- User registration schemas

## Prompt
In `backend/app/modules/auth/schemas.py`, replace TODO placeholder with 3 Pydantic v2 schemas:
- LoginRequest (username, password)
- TokenResponse (access_token, token_type)
- MeResponse (user dict, roles list, permissions list)

Do NOT:
- Add password validators
- Add other schemas
- Implement endpoints
- Ask clarification questions
## Non-Goals
- Do NOT introduce new frameworks/libraries unless explicitly required by this task.
- Do NOT refactor unrelated modules or perform repository-wide formatting.
- Do NOT change data models / database schema / migrations unless this task explicitly says so.
- Do NOT modify authentication/authorization model beyond the scope explicitly described in this task (if applicable).


## Acceptance checklist
- [ ] Only `auth/schemas.py` modified
- [ ] 3 schemas defined exactly as specified
- [ ] Pydantic v2 compatible
- [ ] LoginRequest strips whitespace
- [ ] Code passes ruff check/format

## Evidence
Provide objective proof of completion (attach/quote in PR or task result):

- Commands executed (lint/test/e2e if applicable) and their outputs (or links to logs).
- Example requests demonstrating the behavior (e.g., curl) and expected responses.
- Test coverage: new/updated test file paths and passing results.
- Code change evidence: `git diff` for files within this task’s scope.


## Validation commands
```bash
cd backend
ruff check app/modules/auth/schemas.py
ruff format app/modules/auth/schemas.py
python -m py_compile app/modules/auth/schemas.py

# Quick test
python -c "
from app.modules.auth.schemas import LoginRequest, TokenResponse, MeResponse
lr = LoginRequest(username='  test  ', password='pass')
assert lr.username == 'test', 'whitespace not stripped'
print('Schemas OK')
"
```
