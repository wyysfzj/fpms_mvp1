# ENH-02-01 — Create case enums (CaseType, FlowDir, PatentCategory)

## Design references
- `docs/00_mvp1_scope.md`
- `docs/03_database_mvp1_subset.md`

## Target
- **File:** `backend/app/modules/cases/enums.py` (NEW)
- **Atomic rule:** create ONLY this file; define ONLY case-related enums.

## Scope decision (MVP1 – FIXED)
- Create ONLY enums for Cases module.
- This task does NOT modify models or add new fields.

## Components to implement (EXACT)
Create `backend/app/modules/cases/enums.py`:

```python
"""Enumerations for Cases module."""
from enum import Enum


class CaseType(str, Enum):
    """Case type classification."""
    NORMAL = "NORMAL"  # Normal application
    PCT_INTL = "PCT_INTL"  # PCT international phase
    PCT_NATL = "PCT_NATL"  # PCT national phase
    PRIORITY = "PRIORITY"  # Priority claim only


class FlowDir(str, Enum):
    """Application flow direction."""
    CN_DOMESTIC = "CN_DOMESTIC"  # China domestic
    CN_OUTBOUND = "CN_OUTBOUND"  # China to foreign
    FOREIGN_INBOUND = "FOREIGN_INBOUND"  # Foreign to China


class PatentCategory(str, Enum):
    """Patent category (type)."""
    INV = "INV"  # Invention patent
    UM = "UM"  # Utility model
    DES = "DES"  # Design patent


class CaseStatus(str, Enum):
    """Case lifecycle status."""
    NOT_FILED = "NOT_FILED"  # Not yet filed
    PENDING = "PENDING"  # Under examination
    GRANTED = "GRANTED"  # Patent granted
    REJECTED = "REJECTED"  # Application rejected
    WITHDRAWN = "WITHDRAWN"  # Application withdrawn
    ABANDONED = "ABANDONED"  # Abandoned
    EXPIRED = "EXPIRED"  # Patent expired
```

## Non-scope (explicitly excluded)
- Modifying Case model
- Adding migration
- Task-specific enums (separate module)

## Prompt
Create `backend/app/modules/cases/enums.py` with 4 enums for Cases module:
- CaseType (NORMAL, PCT_INTL, PCT_NATL, PRIORITY)
- FlowDir (CN_DOMESTIC, CN_OUTBOUND, FOREIGN_INBOUND)
- PatentCategory (INV, UM, DES)
- CaseStatus (NOT_FILED, PENDING, GRANTED, REJECTED, WITHDRAWN, ABANDONED, EXPIRED)

All enums inherit from `str, Enum` for Pydantic compatibility.

Do NOT:
- Modify models
- Create migrations  
- Ask clarification questions
## Non-Goals
- Do NOT introduce new frameworks/libraries unless explicitly required by this task.
- Do NOT refactor unrelated modules or perform repository-wide formatting.
- Do NOT change data models / database schema / migrations unless this task explicitly says so.
- Do NOT modify authentication/authorization model beyond the scope explicitly described in this task (if applicable).


## Acceptance checklist
- [ ] Only `cases/enums.py` created
- [ ] 4 enums defined exactly as specified
- [ ] All inherit from (str, Enum)
- [ ] Code passes ruff check/format

## Evidence
Provide objective proof of completion (attach/quote in PR or task result):

- Commands executed (lint/test/e2e if applicable) and their outputs (or links to logs).
- Example requests demonstrating the behavior (e.g., curl) and expected responses.
- Test coverage: new/updated test file paths and passing results.
- Code change evidence: `git diff` for files within this task’s scope.


## Validation commands
```bash
cd backend
ruff check app/modules/cases/enums.py
ruff format app/modules/cases/enums.py
python -m py_compile app/modules/cases/enums.py

# Quick test
python -c "
from app.modules.cases.enums import CaseType, FlowDir, PatentCategory, Case Status
assert CaseType.NORMAL == 'NORMAL'
assert PatentCategory.INV == 'INV'
print('Enums OK')
"
```
