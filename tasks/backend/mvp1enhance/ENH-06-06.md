# ENH-06-06 — Fees service: fee rate management (list/create/update)

## Context
Review report requires fee rate management (P1) and a service layer (P0).  

## Target (Atomic)
- File: `backend/app/modules/fees/service.py`

## Allowlist
- `backend/app/modules/fees/service.py` ONLY

## Non-scope
- No API changes
- No schema changes
- No DB/migration changes

## Required implementation (EXACT)
Implement in `fees/service.py`:

1) `list_fee_rates(db, *, filters: dict, page: int, page_size: int) -> tuple[list[T_FeeRate], int]`
   - Filters supported: `fee_code`, `fee_type`, `currency`, `enabled`
   - Pagination + total
   - Order by `fee_code ASC`

2) `create_fee_rate(db, *, data: FeeRateCreateIn, actor_id: str | None) -> T_FeeRate`
   - Insert new row with provided fields
   - If enabled not provided, default to True
   - Commit and refresh

3) `update_fee_rate(db, *, rate_id: str, data: FeeRateUpdateIn, actor_id: str | None) -> T_FeeRate`
   - Load by id; NOT_FOUND if missing
   - Update provided fields (exclude unset)
   - Commit and refresh

Notes:
- Keep validation minimal (type/length enforced by Pydantic).
- Avoid DB IntegrityErrors by validating required fields per schema.

## Acceptance checklist
- [ ] Only service.py changed
- [ ] rate list/create/update functions compile and work


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-06-06**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (not assumptions).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-06-06 lint bash -lc "cd backend && ruff check app/modules/fees/service.py"
./scripts/evidence_run.sh ENH-06-06 fmt  bash -lc "cd backend && ruff format app/modules/fees/service.py"
./scripts/evidence_run.sh ENH-06-06 test bash -lc "cd backend && python3 -m py_compile app/modules/fees/service.py"
./scripts/evidence_finalize.sh ENH-06-06
./scripts/task_validate.sh ENH-06-06
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required field/nullability cannot be determined from models/migrations
