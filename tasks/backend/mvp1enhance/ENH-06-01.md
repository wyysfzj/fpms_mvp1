# ENH-06-01 — Fees enums (FeeType, FeeDraftStatus)

## Context
Review report flags missing `FeeType` enum (GOV/SERVICE/MISC) and fee draft locking mechanism. The DB stores fee_type/status as strings; we standardize via enums.  

## Target (Atomic)
- File: `backend/app/modules/fees/enums.py`

## Allowlist
- `backend/app/modules/fees/enums.py` ONLY

## Non-scope
- No schemas/service/API changes
- No DB/migration changes

## Required implementation (EXACT)
Implement in `fees/enums.py` using `Enum` with `str` mixin:

1) `FeeType` with values:
- `GOV`
- `SERVICE`
- `MISC`

2) `FeeDraftStatus` with values:
- `OPEN`
- `LOCKED`

Notes:
- Values MUST match what is persisted in `t_fee_draft.status` and `t_fee_item.fee_type` per migration/docs.

## Acceptance checklist
- [ ] Only enums.py changed
- [ ] `python3 -m py_compile app/modules/fees/enums.py` passes


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-06-01**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (not assumptions).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-06-01 lint bash -lc "cd backend && ruff check app/modules/fees/enums.py"
./scripts/evidence_run.sh ENH-06-01 fmt  bash -lc "cd backend && ruff format app/modules/fees/enums.py"
./scripts/evidence_run.sh ENH-06-01 test bash -lc "cd backend && python3 -m py_compile app/modules/fees/enums.py"
./scripts/evidence_finalize.sh ENH-06-01
./scripts/task_validate.sh ENH-06-01
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required field/nullability cannot be determined from models/migrations
