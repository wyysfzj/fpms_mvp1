# ENH-08-14 — SystemParams — Upsert One (PUT /system/params/{param_key})

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/system/api.py`

## Allowlist
- `backend/app/modules/system/api.py` ONLY

## Endpoint (EXACT)
- Method: PUT
- Path: /system/params/{param_key}
- Permission: `SystemParam.Edit` via `require_perm("SystemParam.Edit")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `SystemParam.Edit`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 200: success

## Required implementation (EXACT)
- Implement ONLY `PUT /system/params/{param_key}`.
- Path param: `param_key`
- Body schema: `SystemParamUpsertIn`
- Call `upsert_system_param(db, param_key=param_key, data=payload, actor_id=current_user.id)`
- Return `OkOut(status="ok")` OR return updated `SystemParamOut` if module convention already returns the entity.
- Ensure behavior is idempotent.

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -X PUT -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"param_value":"true","value_type":"bool","is_secret":false}' \
  "http://localhost:8000/api/v1/system/params/FEATURE_FLAG_X"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-14**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-14 lint bash -lc "cd backend && ruff check app/modules/system/api.py"
./scripts/evidence_run.sh ENH-08-14 fmt  bash -lc "cd backend && ruff format app/modules/system/api.py"
./scripts/evidence_run.sh ENH-08-14 test bash -lc "cd backend && python3 -m py_compile app/modules/system/api.py"
./scripts/evidence_finalize.sh ENH-08-14
./scripts/task_validate.sh ENH-08-14
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
