# ENH-04-08 — Documents API: GET /documents (Doc.Read)

**Source**: `mvp1_backend_review_report.md` §5.4 (service wiring) + `tasks/backend/apis/BE-APIv4-011_documents_get_documents.md`  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Implement `GET /documents` endpoint using the service layer and typed schemas.

## Design references (authoritative)
- `backend/app/modules/documents/docs/doc_02_api.md`
- `tasks/backend/apis/BE-APIv4-011_documents_get_documents.md`
- `docs/02_permissions_rbac.md` (permission enforcement pattern)

## Endpoint (EXACT)
- Method: `GET`
- Path: `/documents`
- Permission: `require_perm("Doc.Read")`
- Success: HTTP `200`

## Expected status codes (realistic)
- 401: missing/invalid token
- 403: authenticated but missing `Doc.Read`
- 200: success

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/modules/documents/api.py`.

## Allowed files (Strict allowlist)
- `backend/app/modules/documents/api.py`

## Requirements (EXACT)
In `documents/api.py`, implement ONLY this endpoint:
- `GET /documents`
- Use `require_perm("Doc.Read")`
- Use the service `list_documents` (ENH-04-04)
- Response model must match `doc_02_api.md` (pagination fields: items/page/page_size/total)
- Implement query params for filtering as per `doc_02_api.md` (at minimum: q, direction, case_id, doc_template_id, date_from/date_to, page/page_size)

Hard rules:
- Do NOT add other endpoints in this task
- Do NOT modify schemas/service/models in this task (assume prior tasks completed)

## Evidence required
Provide at least:
- Ruff + py_compile logs
- One curl example showing 401 or 403 is fine before permission seed; 200 expected after Admin perms are synced.

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/documents"
```

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-08 lint bash -lc "cd backend && ruff check app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-08 fmt  bash -lc "cd backend && ruff format app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-08 test bash -lc "cd backend && python3 -m py_compile app/modules/documents/api.py"
./scripts/evidence_finalize.sh ENH-04-08
./scripts/task_validate.sh ENH-04-08
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/modules/documents/api.py`

