# ENH-07-05 — Clients service: update + client_code uniqueness validation

## Context
Service layer must support update without direct DB writes in API, and must preserve uniqueness constraints.

## Target (Atomic)
- File: `backend/app/modules/masterdata/clients/service.py`

## Allowlist
- `backend/app/modules/masterdata/clients/service.py` ONLY

## Non-scope
- No API changes
- No schema changes
- No DB/migration changes

## Required implementation (EXACT)
Implement:

1) `update_client(db, *, client_id: str, data: ClientUpdateIn, actor_id: str | None) -> Client`
   - Load existing client (reuse `get_client`)
   - If `client_code` is being changed, call:
     `assert_client_code_unique(db, client_code=new_code, exclude_client_id=client_id)`
   - Update only provided fields (`exclude_unset=True`)
   - Commit and refresh
   - Return updated ORM object

## Acceptance checklist
- [ ] Only service.py changed
- [ ] Update rejects duplicate client_code (BusinessError)
- [ ] Update commits and returns updated client


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-07-05**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (design docs / migrations / ORM models).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-07-05 lint bash -lc "cd backend && ruff check app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-05 fmt  bash -lc "cd backend && ruff format app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-05 test bash -lc "cd backend && python3 -m py_compile app/modules/masterdata/clients/service.py"
./scripts/evidence_finalize.sh ENH-07-05
./scripts/task_validate.sh ENH-07-05
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required fields/nullability cannot be determined from ORM models/migrations
- The endpoint path/prefix conflicts with existing router wiring
