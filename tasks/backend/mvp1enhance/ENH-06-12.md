# ENH-06-12 — PUT /fees/drafts/{id}/items/{item_id} (edit fee item)

## Context
Implement Fees API endpoint per Fees module design docs and review report. This endpoint must delegate to `fees/service.py` and use schemas from `fees/schemas.py`.

## Target (Atomic)
- File: `backend/app/modules/fees/api.py`

## Allowlist
- `backend/app/modules/fees/api.py` ONLY

## Non-scope
- Do NOT modify service.py / schemas.py
- Do NOT change DB/migrations
- Do NOT add unrelated endpoints

## Endpoint (EXACT)
- Method: PUT
- Path: /fees/drafts/{id}/items/{item_id}
- Permission: `Fee.Edit` enforced via `require_perm("Fee.Edit")`
- Success status: 200

## Expected status codes (realistic)
- 401: missing/invalid token
- 403: missing permission `Fee.Edit`
- 404: resource not found (if applicable)
- 409/400: business rule violation (e.g., draft locked) depending on your BusinessError mapping
- 422: schema validation failure
- 200: success

## Required implementation (EXACT)
- Implement ONLY `PUT /fees/drafts/{id}/items/{item_id}`.
- Path params: `id` (draft_id), `item_id`
- Body schema: `FeeItemUpdateIn`
- Call `update_fee_item(db, draft_id=id, item_id=item_id, data=payload, actor_id=current_user.id)`
- Return `FeeItemOut`

## Validation
```bash
cd backend
ruff check app/modules/fees/api.py
python3 -m py_compile app/modules/fees/api.py
```

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
export DRAFT_ID="REPLACE_DRAFT_ID"
export ITEM_ID="REPLACE_ITEM_ID"
curl -i -X PUT -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"quantity":"2"}' \
  "http://localhost:8000/api/v1/fees/drafts/$DRAFT_ID/items/$ITEM_ID"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-06-12**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (not assumptions).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-06-12 lint bash -lc "cd backend && ruff check app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-12 fmt  bash -lc "cd backend && ruff format app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-12 test bash -lc "cd backend && python3 -m py_compile app/modules/fees/api.py"
./scripts/evidence_finalize.sh ENH-06-12
./scripts/task_validate.sh ENH-06-12
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required field/nullability cannot be determined from models/migrations
