# ENH-06-08 — POST /fees/drafts (create fee draft)

## Context
Implement Fees API endpoint per Fees module design docs and review report. This endpoint must delegate to `fees/service.py` and use schemas from `fees/schemas.py`.

## Target (Atomic)
- File: `backend/app/modules/fees/api.py`

## Allowlist
- `backend/app/modules/fees/api.py` ONLY

## Non-scope
- Do NOT modify service.py / schemas.py
- Do NOT change DB/migrations
- Do NOT add unrelated endpoints

## Endpoint (EXACT)
- Method: POST
- Path: /fees/drafts
- Permission: `Fee.Create` enforced via `require_perm("Fee.Create")`
- Success status: 201

## Expected status codes (realistic)
- 401: missing/invalid token
- 403: missing permission `Fee.Create`
- 404: resource not found (if applicable)
- 409/400: business rule violation (e.g., draft locked) depending on your BusinessError mapping
- 422: schema validation failure
- 201: success

## Required implementation (EXACT)
- Implement ONLY `POST /fees/drafts`.
- Body schema: `FeeDraftCreateIn`
- Call `create_fee_draft(db, data=payload, actor_id=current_user.id)`
- Return `FeeDraftOut`
- Ensure HTTP 201.

## Validation
```bash
cd backend
ruff check app/modules/fees/api.py
python3 -m py_compile app/modules/fees/api.py
```

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
export CASE_ID="REPLACE_CASE_ID"
export CLIENT_ID="REPLACE_CLIENT_ID"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"case_id":"'"$CASE_ID"'","client_id":"'"$CLIENT_ID"'","currency":"CNY"}' \
  "http://localhost:8000/api/v1/fees/drafts"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-06-08**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (not assumptions).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-06-08 lint bash -lc "cd backend && ruff check app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-08 fmt  bash -lc "cd backend && ruff format app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-08 test bash -lc "cd backend && python3 -m py_compile app/modules/fees/api.py"
./scripts/evidence_finalize.sh ENH-06-08
./scripts/task_validate.sh ENH-06-08
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required field/nullability cannot be determined from models/migrations
