# ENH-08-07 — Templates — List (GET /templates)

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/templates/api.py`

## Allowlist
- `backend/app/modules/templates/api.py` ONLY

## Endpoint (EXACT)
- Method: GET
- Path: /templates
- Permission: `Template.Read` via `require_perm("Template.Read")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `Template.Read`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 200: success

## Required implementation (EXACT)
- Implement ONLY `GET /templates`.
- Query params: `page`, `page_size`, optional `group`, `language`, `enabled`
- Call `list_templates(db, filters=..., page=..., page_size=...)`
- Return PageResult-shaped response: `{items, page, page_size, total}`
- Serialize using `TemplateListItemOut` (or `TemplateOut` if design docs demand full objects)

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/templates?page=1&page_size=20"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-07**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-07 lint bash -lc "cd backend && ruff check app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-07 fmt  bash -lc "cd backend && ruff format app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-07 test bash -lc "cd backend && python3 -m py_compile app/modules/templates/api.py"
./scripts/evidence_finalize.sh ENH-08-07
./scripts/task_validate.sh ENH-08-07
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
