# ENH-12-04 — Documentation: deployment runbook (MVP1)

## Context / Why
Review report §6.4 item 12.4 requires a deployment runbook.
This must be actionable and match the repo’s actual execution model (FastAPI + Alembic + seed script).

## Target (Atomic)
- File: `docs/deployment_runbook.md`

## Allowed files (Strict allowlist)
- `docs/deployment_runbook.md`

## Required change (EXACT)
Create `docs/deployment_runbook.md` with:

1) `# FPMS MVP1 Deployment Runbook`
2) `## Deployment prerequisites`
   - OS/runtime assumptions (Python version, DB engine)
   - Required environment variables (link to docs/environment_variables.md)

3) `## Build / Install`
   - How to install backend deps (match repo tooling: requirements/Poetry/uv; do NOT guess)

4) `## Database migration`
   - `cd backend && alembic upgrade head`
   - How to verify alembic version table
   - Rollback guidance (`alembic downgrade -1`) with warning

5) `## Seed data`
   - How to run seed script(s) (use real script paths)
   - Mention that roles/permissions seeding and admin user creation are required for smoke tests

6) `## Start the service`
   - `uvicorn app.main:app ...` (match repo layout)
   - Mention reverse proxy concerns (future), but keep MVP1 minimal

7) `## Post-deploy smoke checks`
   - `/healthz`
   - `/docs` reachable
   - Basic authenticated call (e.g., `/auth/me`)
   - Permissions sync verification

8) `## Backup / Restore (MVP1 pragmatic)`
   - SQLite: copy file (dev)
   - For production DB: placeholder instructions (do not claim a specific DB unless repo already uses it)

9) `## Troubleshooting`
   - Common startup errors (import path, missing env vars)
   - 401/403 issues due to perms/seed mismatch

Formatting rules:
- Use fenced code blocks for commands.
- Do not include local absolute file paths (no `file:///Users/...`).

## Acceptance checklist
- [ ] Only `docs/deployment_runbook.md` is created/modified
- [ ] Runbook includes migrate + seed + start + smoke checks
- [ ] No invented tooling; commands match the repo

## Evidence (MUST RUN)
```bash
./scripts/evidence_run.sh ENH-12-04 test bash -lc "test -f docs/deployment_runbook.md"
./scripts/evidence_run.sh ENH-12-04 test bash -lc "python3 - <<'PY'\nfrom pathlib import Path\np=Path('docs/deployment_runbook.md')\ntext=p.read_text(encoding='utf-8')\nreq=['# FPMS MVP1 Deployment Runbook','## Database migration','## Seed data','## Post-deploy smoke checks']\nmissing=[s for s in req if s not in text]\nprint('missing:', missing)\nassert not missing\nprint('OK')\nPY"
./scripts/evidence_finalize.sh ENH-12-04
./scripts/task_validate.sh ENH-12-04
```

## STOP contract
STOP if:
- Any other file besides `docs/deployment_runbook.md` must be changed
- You cannot determine the repo’s actual dependency management tooling
- You need to change Docker/infra files (out of scope)

---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-12-04**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add features beyond this task.
- Use in-repo docs as source-of-truth.

