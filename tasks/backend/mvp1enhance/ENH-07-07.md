# ENH-07-07 — GET /clients (list clients)

## Context
Implement Clients API endpoint using schemas and service layer (no direct DB access in API). Align with `masterdata/clients/docs/client_00_overview.md`.

## Target (Atomic)
- File: `backend/app/modules/masterdata/clients/api.py`

## Allowlist
- `backend/app/modules/masterdata/clients/api.py` ONLY

## Non-scope
- Do NOT modify service.py / schemas.py
- Do NOT change DB/migrations
- Do NOT add unrelated endpoints

## Endpoint (EXACT)
- Method: GET
- Path: /clients
- Permission: `Client.Read` via `require_perm("Client.Read")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `Client.Read`
- 404: resource not found (where applicable)
- 409/400: business rule violation (e.g., client_code duplicate) depending on BusinessError mapping
- 422: schema validation failure
- 200: success

## Required implementation (EXACT)
- Implement ONLY `GET /clients`.
- Query params: `page`, `page_size`, optional `q`, `is_active`
- Call `list_clients(...)` from service
- Return PageResult shape: `{items, page, page_size, total}`
- Serialize each item using `ClientListItemOut` (or `ClientOut` if design doc mandates).

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/clients?page=1&page_size=20"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-07-07**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (design docs / migrations / ORM models).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-07-07 lint bash -lc "cd backend && ruff check app/modules/masterdata/clients/api.py"
./scripts/evidence_run.sh ENH-07-07 fmt  bash -lc "cd backend && ruff format app/modules/masterdata/clients/api.py"
./scripts/evidence_run.sh ENH-07-07 test bash -lc "cd backend && python3 -m py_compile app/modules/masterdata/clients/api.py"
./scripts/evidence_finalize.sh ENH-07-07
./scripts/task_validate.sh ENH-07-07
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required fields/nullability cannot be determined from ORM models/migrations
- The endpoint path/prefix conflicts with existing router wiring
