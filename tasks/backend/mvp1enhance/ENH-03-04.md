
# ENH-03-04 — Implement Currency Consistency Validation

## Goal
Implement validation logic to ensure that the **currency** used in the bill is consistent across all bill items.

## Scope
- Modify **ONLY** `service.py` to implement the currency consistency check.

## Required Steps
1) In `service.py`, implement the currency consistency validation logic to check that the currency used in the bill matches the currency for all bill items.
2) Add relevant error handling if the validation fails.

## Acceptance Criteria
- [ ] Validation is added to check for consistent currency in the bill and its items.
- [ ] Proper error handling and messaging when validation fails.

## Evidence Required
- `ruff check` and `python3 -m py_compile` outputs for `service.py`.
- `git diff` showing only the changes in `service.py`.

## Verification
```bash
cd backend
ruff check app/modules/billing/service.py
ruff format app/modules/billing/service.py
python3 -m py_compile app/modules/billing/service.py
cd ..
```

## Codex Execution Prompt (v2)

You are a coding agent executing **exactly ONE** atomic task.

## Task File (Authoritative)
tasks/backend/mvp1enhance/ENH-03-04.md

## Goal
Implement currency consistency validation for the bill and its items.

## Hard Rules (MUST FOLLOW)
- Modify **ONLY** `service.py` to implement the currency consistency validation.
- Do **NOT** modify any other files outside `service.py`.

## Allowed Files (Strict Allowlist)
- `backend/app/modules/billing/service.py`

## Required Steps
1) In `service.py`, implement logic to validate that the bill and its items have consistent currencies.
2) Add proper error handling when validation fails.

## Verification (MUST RUN)
```bash
cd backend
ruff check app/modules/billing/service.py
ruff format app/modules/billing/service.py
python3 -m py_compile app/modules/billing/service.py
cd ..
```

Evidence Required
-----------------
Provide:
- `git diff` showing the changes in `service.py`.
- Successful execution of validation commands.

Completion Criteria
-------------------
Task is DONE only if:
- The currency consistency validation is implemented and validated.

STOP Contract
-------------
STOP immediately if:
- You need to modify any file outside `service.py`.
- Validation fails for the currency consistency logic.
