# ENH-04-09 — Documents API: POST /documents (Doc.Create)

**Source**: Review §5.4 + `tasks/backend/apis/BE-APIv4-012_documents_post_documents.md`  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Implement `POST /documents` endpoint using typed request schema and service-layer creation.

## Design references (authoritative)
- `backend/app/modules/documents/docs/doc_02_api.md`
- `tasks/backend/apis/BE-APIv4-012_documents_post_documents.md`
- `docs/02_permissions_rbac.md`

## Endpoint (EXACT)
- Method: `POST`
- Path: `/documents`
- Permission: `require_perm("Doc.Create")`
- Success: HTTP `201`

## Expected status codes
- 401: missing/invalid token
- 403: authenticated but missing `Doc.Create`
- 422: request body validation failure (with valid token + permission)
- 201: created

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/modules/documents/api.py`.

## Allowed files (Strict allowlist)
- `backend/app/modules/documents/api.py`

## Requirements (EXACT)
In `documents/api.py`, implement ONLY this endpoint:
- `POST /documents`
- Use `require_perm("Doc.Create")`
- Body: `DocumentCreateIn` (ENH-04-03)
- Call service `create_document` (ENH-04-05)
- Return `DocumentOut` (ENH-04-03)
- Ensure response uses HTTP 201

Hard rules:
- Do NOT add other endpoints
- Do NOT modify schemas/service/models

## Evidence required
- Ruff + py_compile logs
- Curl example:
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{}' \
  "http://localhost:8000/api/v1/documents"
```
(422 expected for empty body once auth+perm are satisfied.)

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-09 lint bash -lc "cd backend && ruff check app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-09 fmt  bash -lc "cd backend && ruff format app/modules/documents/api.py"
./scripts/evidence_run.sh ENH-04-09 test bash -lc "cd backend && python3 -m py_compile app/modules/documents/api.py"
./scripts/evidence_finalize.sh ENH-04-09
./scripts/task_validate.sh ENH-04-09
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/modules/documents/api.py`

