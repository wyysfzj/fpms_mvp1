# ENH-04-14 — Wire Documents router into main API router

**Source**: `mvp1_backend_review_report.md` (deployment readiness) + blueprint execution order  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Ensure Documents module endpoints are reachable by wiring the documents router into the global API router.

## Design references
- `docs/execution_order_v2.md` (router wiring sequence)
- Existing router patterns in `backend/app/api/router.py`

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/api/router.py`.

## Allowed files (Strict allowlist)
- `backend/app/api/router.py`

## Requirements (EXACT)
In `app/api/router.py`:
- Import the Documents router (module path per project convention), commonly:
  - `from app.modules.documents.api import router as documents_router`
- Include it under:
  - prefix: `/documents`
  - tags: `["documents"]`

Hard rules:
- Do NOT change other routers
- Do NOT reorder routers unless required by existing conventions

## Acceptance checklist
- [ ] Only `app/api/router.py` changed
- [ ] Documents router included under `/documents`
- [ ] Backend imports compile
- [ ] Ruff + py_compile pass; task gate passes

## Evidence required
- Ruff + py_compile logs
- Optional: `uvicorn app.main:app` starts without import errors

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-14 lint bash -lc "cd backend && ruff check app/api/router.py"
./scripts/evidence_run.sh ENH-04-14 fmt  bash -lc "cd backend && ruff format app/api/router.py"
./scripts/evidence_run.sh ENH-04-14 test bash -lc "cd backend && python3 -m py_compile app/api/router.py"
./scripts/evidence_finalize.sh ENH-04-14
./scripts/task_validate.sh ENH-04-14
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/api/router.py`

