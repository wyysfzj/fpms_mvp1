# ENH-01-06 — Implement authenticate_user service

## Design references
- `docs/04_backend_architecture.md`
- `tasks/backend_tasks_atomic.md` (BE-01-08, BE-01-09)

## Target
- **File:** `backend/app/modules/auth/service.py`
- **Atomic rule:** modify ONLY this file; implement ONLY authenticate_user function.

## Prerequisites
- ENH-00-05 complete (security.py with verify_password exists)

## Scope decision (MVP1 – FIXED)
- Implement ONLY `authenticate_user()` function.
- This task does NOT implement login endpoint.

## Components to implement (EXACT)
Replace TODO placeholder in `auth/service.py`:

```python
from sqlalchemy.orm import Session
from app.core.security import verify_password
from app.modules.auth.models import T_User


def authenticate_user(db: Session, username: str, password: str) -> T_User | None:
    """
    Authenticate user with username and password.
    
    Returns:
        T_User if authenticated, None otherwise
    """
    # Find user by username
    user = db.query(T_User).filter(T_User.username == username).first()
    
    if not user:
        return None
    
    # Check if active
    if not user.is_active:
        return None
    
    # Verify password
    if not verify_password(password, user.password_hash):
        return None
    
    return user
```

## Non-scope (explicitly excluded)
- Login endpoint implementation
- Token generation (separate endpoint task)
- Account lockout logic
- Password reset logic

## Prompt
In `backend/app/modules/auth/service.py`, replace TODO with `authenticate_user(db, username, password)` function.

Requirements:
- Query user by username
- Check is_active flag  
- Verify password using security.verify_password
- Return user or None

Do NOT:
- Implement login endpoint
- Add account lockout
- Raise exceptions (return None on failure)
- Ask clarification questions
## Non-Goals
- Do NOT introduce new frameworks/libraries unless explicitly required by this task.
- Do NOT refactor unrelated modules or perform repository-wide formatting.
- Do NOT change data models / database schema / migrations unless this task explicitly says so.
- Do NOT modify authentication/authorization model beyond the scope explicitly described in this task (if applicable).


## Acceptance checklist
- [ ] Only `auth/service.py` modified
- [ ] Function signature matches spec
- [ ] Returns T_User | None
- [ ] Checks is_active
- [ ] Uses verify_password from security.py
- [ ] Code passes ruff check/format

## Evidence
Provide objective proof of completion (attach/quote in PR or task result):

- Commands executed (lint/test/e2e if applicable) and their outputs (or links to logs).
- Example requests demonstrating the behavior (e.g., curl) and expected responses.
- Test coverage: new/updated test file paths and passing results.
- Code change evidence: `git diff` for files within this task’s scope.


## Validation commands
```bash
cd backend
ruff check app/modules/auth/service.py
ruff format app/modules/auth/service.py
python -m py_compile app/modules/auth/service.py
```

## Manual test (after user seeded)
```python
from app.db.session import get_db
from app.modules.auth.service import authenticate_user

db = next(get_db())
user = authenticate_user(db, "admin", "admin123")
assert user is not None, "Auth failed"
assert user.username == "admin"

user = authenticate_user(db, "admin", "wrong")
assert user is None, "Should fail with wrong password"
```
