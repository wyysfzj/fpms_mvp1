# ENH-07-06 — Clients service: deactivate (soft disable)

## Context
Client deactivation endpoint exists in API spec. Service must implement action while keeping audit fields intact.

## Target (Atomic)
- File: `backend/app/modules/masterdata/clients/service.py`

## Allowlist
- `backend/app/modules/masterdata/clients/service.py` ONLY

## Non-scope
- No API changes
- No schema changes
- No DB/migration changes

## Required implementation (EXACT)
Implement:

1) `deactivate_client(db, *, client_id: str, actor_id: str | None) -> None`
   - Load client (reuse `get_client`)
   - Set `is_active = False`
   - Commit
   - Idempotent behavior: if already inactive, still return ok (no error)

## Acceptance checklist
- [ ] Only service.py changed
- [ ] Deactivate commits
- [ ] No 500s for already-inactive clients


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-07-06**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (design docs / migrations / ORM models).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-07-06 lint bash -lc "cd backend && ruff check app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-06 fmt  bash -lc "cd backend && ruff format app/modules/masterdata/clients/service.py"
./scripts/evidence_run.sh ENH-07-06 test bash -lc "cd backend && python3 -m py_compile app/modules/masterdata/clients/service.py"
./scripts/evidence_finalize.sh ENH-07-06
./scripts/task_validate.sh ENH-07-06
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required fields/nullability cannot be determined from ORM models/migrations
- The endpoint path/prefix conflicts with existing router wiring
