# ENH-04-01 — Documents: Complete ORM models + minimal relationships (t_doc_template, t_document, t_doc_attachment)

**Source**: `mvp1_backend_review_report.md` §5.4 (Items 4.4, 4.5)  
**Priority**: P0  
**Date**: 2026-01-24

## Goal
Ensure Documents module ORM models are complete and match the migration/spec, and add **minimal** relationships needed for correct document↔case / document↔attachments behavior.

This addresses review findings:
- Missing/partial `T_DocAttachment` model
- Missing relationships to Case (and attachments)

## Design references (authoritative)
- `backend/app/modules/documents/docs/doc_01_db.md`
- `backend/alembic/versions/0002_documents.py` (schema source of truth)
- `tasks/backend/models_from_migrations/BE-MD-0002-02_t_doc_template.md`
- `tasks/backend/models_from_migrations/BE-MD-0002-03_t_document.md`
- `tasks/backend/models_from_migrations/BE-MD-0002-01_t_doc_attachment.md`

If any conflict exists, **0002_documents.py wins**.

## Scope (Atomic – FIXED)
Modify **ONLY** `backend/app/modules/documents/models.py`.

## Allowed files (Strict allowlist)
- `backend/app/modules/documents/models.py`

## Requirements (EXACT)
In `documents/models.py`:

### A) Tables / columns must match migration
Ensure ORM classes map to these tables (names must match migration):
- `t_doc_template` (id, code, name, direction, enabled)
- `t_document` (id, case_id, doc_template_id, direction, doc_date, title, ref_no, extra_data, created_at, updated_at)
- `t_doc_attachment` (id, document_id, file_name, file_path, mime_type, file_size, uploaded_at)

Rules:
- Do **NOT** add columns not present in migration.
- Column names must match exactly.
- FK constraints should match migration (case_id -> t_case.id, doc_template_id -> t_doc_template.id, document_id -> t_document.id).

### B) Minimal relationships (no cross-file changes)
Add relationships **only within this file** and avoid requiring edits in other modules:
- Document -> attachments: `attachments = relationship("...", ...)` using string targets
- Attachment -> document: `document = relationship("...", ...)` using string targets
- Document -> case: add a relationship using a string target (no back_populates required)

**Hard rule**: Do NOT require changes in `cases/models.py` (no back_populates).

### C) Enums
Do not introduce SA Enum columns (migration uses String). Direction validation will be handled by Pydantic (next tasks).

## Acceptance checklist
- [ ] Only `documents/models.py` changed
- [ ] All three tables map to the correct `__tablename__` values
- [ ] Columns/FKs match `0002_documents.py` exactly
- [ ] Minimal relationships exist without requiring edits in other modules
- [ ] File imports/compiles successfully

## Evidence required
- `git diff` limited to `documents/models.py`
- Ruff + py_compile logs

## Codex / Agent Execution Prompt (AI‑EOS v2 — Bounded Executor)

You are a coding agent executing **exactly ONE** atomic task from this file.

**Role**: bounded executor.
- Implement exactly what this task requires.
- Make the smallest change that satisfies Acceptance.
- Collect objective evidence via EOS scripts.
- STOP immediately if requirements exceed the allowlist.

You MUST NOT:
- Expand scope (“nice-to-have”, refactors, unrelated cleanups)
- Add new endpoints not requested by this task
- Change DB schema/migrations unless the task explicitly says so
- Propose follow-up tasks; this task must stand on its own


## Implementation constraints (Hard)
- Modify ONLY the allowlisted file.
- Do not rename existing ORM classes if they already exist (preserve API compatibility); adjust mapping/columns instead.

## Validation (MUST RUN via EOS scripts)
```bash
./scripts/evidence_run.sh ENH-04-01 lint bash -lc "cd backend && ruff check app/modules/documents/models.py"
./scripts/evidence_run.sh ENH-04-01 fmt  bash -lc "cd backend && ruff format app/modules/documents/models.py"
./scripts/evidence_run.sh ENH-04-01 test bash -lc "cd backend && python3 -m py_compile app/modules/documents/models.py"
./scripts/evidence_finalize.sh ENH-04-01
./scripts/task_validate.sh ENH-04-01
```


## STOP Contract
STOP immediately and report if:
- Any requirement cannot be met without modifying files outside the allowlist
- Design docs conflict in a way that blocks implementation
- You are tempted to refactor unrelated code

**Allowlist**:
- `backend/app/modules/documents/models.py`

