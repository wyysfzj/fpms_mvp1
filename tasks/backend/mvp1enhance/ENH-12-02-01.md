# ENH-12-02-01 — Docs: fix API usage guide to avoid non-existent /auth/me endpoint

## Context / Why
While executing `ENH-12-02`, the agent correctly STOPPED because the guide referenced `GET /auth/me`,
but that endpoint cannot be confirmed in the repository code (no route in auth api.py).

Documentation must only describe **implemented** endpoints to prevent false guidance.

## Target (Atomic)
Update the API usage guide to remove/replace the `/auth/me` section with a verified token validation approach.

## Allowed files (Strict allowlist)
- `docs/api_usage_guide.md` (MODIFY) ONLY

## Non-scope
- Do NOT implement new endpoints
- Do NOT modify backend code
- Do NOT modify any other docs

## Required change (EXACT)
In `docs/api_usage_guide.md`:

1) Remove any section that documents or gives curl examples for:
- `GET /api/v1/auth/me`
- or any `/auth/me` variant

2) Replace it with a **Token Validation / WhoAmI (without /auth/me)** section that:
- Explains there is currently **no** `/auth/me` endpoint in MVP1.
- Provides a safe, repository-agnostic validation method:
  - Call any existing protected list endpoint; if it returns **200**, token is valid and permitted.
  - If it returns **403**, token is valid but lacks permission (suggest `scan_perms.py` + `seed_dev.py` + re-login).
  - If it returns **401**, token is missing/invalid/expired.

3) Provide at least ONE curl example using a commonly existing endpoint:
- Prefer `GET /api/v1/documents?page=1&page_size=20` OR `GET /api/v1/clients?page=1&page_size=20`
- The guide MUST NOT claim the endpoint exists unless it is already used elsewhere in the guide and known in this repo.
  - If both are present in the guide, pick one and keep it consistent.

4) Keep the rest of the guide unchanged.

## Acceptance checklist
- [ ] Only `docs/api_usage_guide.md` modified
- [ ] No mention of `/auth/me` remains
- [ ] Token validation instructions are accurate and do not require new endpoints

## Evidence (MUST RUN)
```bash
./scripts/evidence_run.sh ENH-12-02-01 quick bash -lc "test -f docs/api_usage_guide.md && grep -R \"/auth/me\" -n docs/api_usage_guide.md && exit 1 || echo OK"
./scripts/evidence_finalize.sh ENH-12-02-01
./scripts/task_validate.sh ENH-12-02-01
```

## STOP Contract
STOP if any file other than `docs/api_usage_guide.md` is required.

---

## Codex Execution Prompt (AI‑EOS)
Execute exactly ONE atomic task: **ENH-12-02-01**.

- Modify ONLY `docs/api_usage_guide.md`.
- Remove `/auth/me` documentation and replace with a token validation section that uses an existing protected endpoint.
- Do NOT add or change backend endpoints.
- Run the Evidence commands and ensure `task_validate.sh ENH-12-02-01` passes.
