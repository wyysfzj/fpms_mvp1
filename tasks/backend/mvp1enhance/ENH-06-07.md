# ENH-06-07 — GET /fees/drafts (list fee drafts)

## Context
Implement Fees API endpoint per Fees module design docs and review report. This endpoint must delegate to `fees/service.py` and use schemas from `fees/schemas.py`.

## Target (Atomic)
- File: `backend/app/modules/fees/api.py`

## Allowlist
- `backend/app/modules/fees/api.py` ONLY

## Non-scope
- Do NOT modify service.py / schemas.py
- Do NOT change DB/migrations
- Do NOT add unrelated endpoints

## Endpoint (EXACT)
- Method: GET
- Path: /fees/drafts
- Permission: `Fee.Read` enforced via `require_perm("Fee.Read")`
- Success status: 200

## Expected status codes (realistic)
- 401: missing/invalid token
- 403: missing permission `Fee.Read`
- 404: resource not found (if applicable)
- 409/400: business rule violation (e.g., draft locked) depending on your BusinessError mapping
- 422: schema validation failure
- 200: success

## Required implementation (EXACT)
- Implement ONLY `GET /fees/drafts`.
- Query params: `page`, `page_size`, optional filters `case_id`, `client_id`, `status`
- Call `list_fee_drafts(db, filters=..., page=..., page_size=...)`
- Return a PageResult-shaped response:
  `{ "items": [...], "page": ..., "page_size": ..., "total": ... }`
- Serialize items using `FeeDraftListItemOut` (or `FeeDraftOut` if your API doc requires).

## Validation
```bash
cd backend
ruff check app/modules/fees/api.py
python3 -m py_compile app/modules/fees/api.py
```

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/fees/drafts?page=1&page_size=20"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are a coding agent executing exactly ONE atomic task: **ENH-06-07**.

### Hard rules
- Modify ONLY the allowlisted file(s) for this task.
- Do NOT refactor unrelated code.
- Do NOT add endpoints/functions not explicitly listed.
- Use in-repo docs as source-of-truth (not assumptions).

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-06-07 lint bash -lc "cd backend && ruff check app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-07 fmt  bash -lc "cd backend && ruff format app/modules/fees/api.py"
./scripts/evidence_run.sh ENH-06-07 test bash -lc "cd backend && python3 -m py_compile app/modules/fees/api.py"
./scripts/evidence_finalize.sh ENH-06-07
./scripts/task_validate.sh ENH-06-07
```

### STOP contract
STOP if:
- You would need to change any file outside the allowlist
- Any required field/nullability cannot be determined from models/migrations
