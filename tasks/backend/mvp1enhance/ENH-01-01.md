# ENH-01-01 — Add T_RolePerm model to rbac/models.py

## Design references
- `docs/02_permissions_rbac.md`
- `tasks/backend_tasks_atomic.md` (BE-01-04)
- `mvp1_backend_review_report.md` (Section 1.1)

## Target
- **File:** `backend/app/modules/rbac/models.py`
- **Atomic rule:** modify ONLY this file; add ONLY T_RolePerm model.

## Prerequisites
- T_Role model exists in same file

## Scope decision (MVP1 – FIXED)
- Add ONLY the T_RolePerm model for role-permission mapping.
- This task does NOT modify existing T_Role, T_UserRole models.
- No migration in this task (separate task).

## Components to implement (EXACT)
Add to `backend/app/modules/rbac/models.py`:

```python
class T_RolePerm(Base):
    """Maps permissions to roles."""
    __tablename__ = "t_role_perm"
    
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=lambda: str(uuid4()))
    role_id: Mapped[str] = mapped_column(String(36), ForeignKey("t_role.id", ondelete="CASCADE"), index=True)
    perm_code: Mapped[str] = mapped_column(String(128), nullable=False, index=True)
    
    __table_args__ = (
        UniqueConstraint('role_id', 'perm_code', name='uq_role_perm'),
    )
```

## Non-scope (explicitly excluded)
- Modifying T_Role or T_UserRole
- Creating Alembic migration
- Adding permissions seeding logic
- Adding relationships to existing models

## Prompt
In `backend/app/modules/rbac/models.py`, add ONLY the `T_RolePerm` model class.

Requirements:
- UUID primary key as String(36)
- Foreign key to t_role.id with CASCADE delete
- Indexed columns: role_id, perm_code
- Unique constraint on (role_id, perm_code)
- perm_code stores strings like "Case.Read", "Bill.Create"

Do NOT:
- Modify existing models
- Create migration file
- Add seeding logic
- Ask clarification questions
## Non-Goals
- Do NOT introduce new frameworks/libraries unless explicitly required by this task.
- Do NOT refactor unrelated modules or perform repository-wide formatting.
- Do NOT change data models / database schema / migrations unless this task explicitly says so.
- Do NOT modify authentication/authorization model beyond the scope explicitly described in this task (if applicable).


## Acceptance checklist
- [ ] Only `backend/app/modules/rbac/models.py` was modified
- [ ] T_RolePerm model added with exact fields
- [ ] Unique constraint on (role_id, perm_code)
- [ ] Existing models unchanged
- [ ] Code passes ruff check/format

## Evidence
Provide objective proof of completion (attach/quote in PR or task result):

- Commands executed (lint/test/e2e if applicable) and their outputs (or links to logs).
- Example requests demonstrating the behavior (e.g., curl) and expected responses.
- Test coverage: new/updated test file paths and passing results.
- Code change evidence: `git diff` for files within this task’s scope.


## Validation commands
```bash
cd backend
ruff check app/modules/rbac/models.py
ruff format app/modules/rbac/models.py
python -m py_compile app/modules/rbac/models.py
```

## Permission code examples (from spec)
- `Case.Read`, `Case.Create`, `Case.Edit`, `Case.EditLimited`
- `Bill.Read`, `Bill.Create`, `Bill.Print`
- `Client.Manage`
- `User.Manage`, `Role.Manage`
