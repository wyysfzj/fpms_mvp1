# ENH-08-12 — LetterHeads — Update (PUT /letterheads/{id})

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/templates/api.py`

## Allowlist
- `backend/app/modules/templates/api.py` ONLY

## Endpoint (EXACT)
- Method: PUT
- Path: /letterheads/{id}
- Permission: `LetterHead.Edit` via `require_perm("LetterHead.Edit")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `LetterHead.Edit`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 200: success

## Required implementation (EXACT)
- Implement ONLY `PUT /letterheads/{id}`.
- Path param: `id` (letterhead_id)
- Body schema: `LetterHeadUpdateIn`
- Call `update_letterhead(db, letterhead_id=id, data=payload, actor_id=current_user.id)`
- Return `LetterHeadOut`

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
export LETTERHEAD_ID="REPLACE_LETTERHEAD_ID"
curl -i -X PUT -H "Authorization: Bearer $FPMS_TOKEN" -H "Content-Type: application/json" \
  -d '{"footer_text":"Updated footer"}' \
  "http://localhost:8000/api/v1/letterheads/$LETTERHEAD_ID"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-12**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-12 lint bash -lc "cd backend && ruff check app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-12 fmt  bash -lc "cd backend && ruff format app/modules/templates/api.py"
./scripts/evidence_run.sh ENH-08-12 test bash -lc "cd backend && python3 -m py_compile app/modules/templates/api.py"
./scripts/evidence_finalize.sh ENH-08-12
./scripts/task_validate.sh ENH-08-12
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
