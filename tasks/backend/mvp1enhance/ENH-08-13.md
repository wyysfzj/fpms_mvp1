# ENH-08-13 — SystemParams — Get All (GET /system/params)

## Context
Implement exactly ONE endpoint for System & Templates module, delegating to service layer and using schemas (thin API).

## Target (Atomic)
- File: `backend/app/modules/system/api.py`

## Allowlist
- `backend/app/modules/system/api.py` ONLY

## Endpoint (EXACT)
- Method: GET
- Path: /system/params
- Permission: `SystemParam.Read` via `require_perm("SystemParam.Read")`
- Success status: 200

## Expected status codes
- 401: missing/invalid token
- 403: missing permission `SystemParam.Read`
- 404: not found (where applicable)
- 409/400: business rule conflict (e.g., duplicate/default rule) depending on BusinessError mapping
- 422: validation error
- 200: success

## Required implementation (EXACT)
- Implement ONLY `GET /system/params`.
- Call `list_system_params(db)`
- For each param, apply masking:
  - if is_secret: return param_value as "******"
- Return list[SystemParamListItemOut] (or SystemParamOut if design requires full)

## Curl example
```bash
export FPMS_TOKEN="REPLACE_ME"
curl -i -H "Authorization: Bearer $FPMS_TOKEN" "http://localhost:8000/api/v1/system/params"
```


---

## Codex / Agent Execution Prompt (AI‑EOS v2)

You are executing exactly ONE atomic task: **ENH-08-13**.

### Hard rules
- Modify ONLY the allowlisted file(s).
- Do NOT refactor unrelated code.
- Do NOT add extra endpoints or behaviors beyond this task.
- Use in-repo design docs and existing patterns as source-of-truth.

### Evidence (MUST RUN)
From repo root:

```bash
./scripts/evidence_run.sh ENH-08-13 lint bash -lc "cd backend && ruff check app/modules/system/api.py"
./scripts/evidence_run.sh ENH-08-13 fmt  bash -lc "cd backend && ruff format app/modules/system/api.py"
./scripts/evidence_run.sh ENH-08-13 test bash -lc "cd backend && python3 -m py_compile app/modules/system/api.py"
./scripts/evidence_finalize.sh ENH-08-13
./scripts/task_validate.sh ENH-08-13
```

### STOP contract
STOP if:
- Any change outside the allowlist is required
- DB/model nullability/PK behavior cannot be determined from existing ORM/migrations
- Router wiring/prefix conflicts would be introduced
