# FPMS V8 Legacy Fee Reduction Provenance Carrier

Status: READY FOR HIGH / ULTRA CONTRACT FROZEN 2026-07-15 / NOT STARTED

## Task Identity

- Task ID: `FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01`
- Risk: `HIGH`
- Execution class: `CONTRACT FROZEN`
- Authority: Delta-4 specification lines 734–749, decision `D4-12`.
- Closure slice: add the append-only `t_legacy_fee_reduction_provenance` carrier through ORM, migration, and one focused schema test.
- Parent revision: `v8_d4_annuity_lineage_01`
- Migration revision: `v8_d4_legacy_fee_provenance_01`
- Story/dependency class: `P0-prereq-heavy-story`

## Dependency Gate

- Require the parent revision `v8_d4_annuity_lineage_01` to exist and be the migration head consumed by this task.
- Do not execute migration work until every prerequisite named by the frozen Delta-4 dependency graph has an independently accepted `PASS`.
- Missing prerequisite evidence, a changed parent head, or an ownership conflict is `BLOCKED`; do not infer or repair dependencies in this task.

## Exact Closure Slice

- Add exactly one append-only `t_legacy_fee_reduction_provenance` carrier through the fee ORM, one migration, and one focused schema test module.
- Close only the exact D4-12 fields, parent/revision lineage, SQLite-safe application-generated UUID identity, `ON DELETE RESTRICT` foreign keys, legacy-value grammar, approval nullability invariant, case/manifest identity, and immutable creation audit stated below.

## Done Definition

1. Add exactly one append-only ORM carrier mapped to `t_legacy_fee_reduction_provenance`.
2. Add migration `v8_d4_legacy_fee_provenance_01` with `down_revision = "v8_d4_annuity_lineage_01"`.
3. The carrier persists exactly these fields:
   - `id`: non-null UUID text primary key, generated by the application ORM default before `INSERT`; the migration has no server-side UUID/default expression.
   - `case_id`: non-null case foreign key with `ON DELETE RESTRICT`.
   - `legacy_value`: non-null string with exact grammar `^(0|0\\.7|0\\.85)$` and no percent, alternate decimal, whitespace, sign, or numeric coercion.
   - `source_reference`: non-null string preserving the authoritative legacy source reference.
   - `source_version`: non-null string preserving the authoritative legacy source version.
   - `source_snapshot_hash`: non-null `String(64)` source-snapshot hash.
   - `manifest_hash`: non-null `String(64)` manifest hash.
   - `confirmed_by`: non-null confirmer foreign key with `ON DELETE RESTRICT`.
   - `confirmed_at`: non-null naive datetime supplied explicitly by the application; no timezone conversion, server default, or automatic `now` value.
   - `approval_id`: nullable approval foreign key with `ON DELETE RESTRICT`.
4. Enforce unique carrier identity across `(case_id, manifest_hash)`.
5. Enforce every D4-12 foreign key—`case_id`, `confirmed_by`, and `approval_id`—with `ON DELETE RESTRICT`; deletion of any referenced row must fail closed.
6. Enforce the exact `legacy_value` grammar at the database boundary and mirror it in the ORM without interpreting the value as a current entitlement or official fee truth.
7. Enforce the bidirectional approval invariant: `approval_id IS NULL` if and only if `legacy_value = '0'`; `approval_id IS NOT NULL` if and only if `legacy_value IN ('0.7', '0.85')`.
8. Treat each persisted carrier row, including confirmation/source/hash fields, as immutable creation audit: no update/delete helper, cascade, mutable business operation, inferred actor, or inferred timestamp is introduced.
9. The focused schema test proves exact table/column shape, application UUID generation, absent server UUID/time defaults, migration lineage, unique `(case_id, manifest_hash)` identity, all restricted foreign keys, exact legacy-value acceptance/rejection, the approval invariant, required confirmation fields, naive `confirmed_at`, and append-only immutability.
10. The schema and test contain none of the rejected alternatives: `fee_schedule_item_id`, percent-valued ratios, source-system/source-record identity, payload fields, guessed creation actor/time, or nullable confirmation.

## Explicit Non-Closure

- No importer, parser, upload route, ingestion job, or legacy data backfill.
- No fee-reduction approval creation/workflow, approval inference, entitlement calculation, customer confirmation, or customer decision.
- No activation of official rates, fee truth, payment truth, or receivable truth.
- No changes to existing fee schedule item semantics, annuity lineage semantics, APIs, UI, seed data, or unrelated migrations.
- No destructive data operation and no downgrade-driven production data policy beyond the repository migration convention.

## Allowed Files

- `tasks/postdemo/v8/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01.md`
- `backend/app/modules/fees/models.py`
- `backend/alembic/versions/v8_delta4_legacy_fee_reduction_provenance.py`
- `backend/tests/test_v8_legacy_fee_reduction_provenance_carrier.py`
- `artifacts/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01/**`

No other file is allowed. Stop and split/replan if another file is required.

## TDD

1. RED: through the migration/ORM public surface, prove exact revision/down-revision and the exact `t_legacy_fee_reduction_provenance` table/column schema.
2. RED: prove the ORM application default generates a UUID text primary key before persistence and SQLite receives no server UUID/default expression.
3. RED: prove only string values `0`, `0.7`, and `0.85` are accepted; reject percent forms, alternate decimals, numbers, signs, and surrounding whitespace.
4. RED: prove `approval_id` is null exactly for `legacy_value = '0'` and non-null exactly for `0.7`/`0.85`.
5. RED: prove duplicate `(case_id, manifest_hash)` fails and deletion across each `case_id`, `confirmed_by`, and `approval_id` relationship is restricted with SQLite foreign keys enabled.
6. RED: prove `confirmed_by` and explicitly supplied naive `confirmed_at` are required, with no guessed actor, server `now`, or timezone-bearing value accepted as stored audit truth.
7. RED: prove carrier rows and their immutable creation-audit fields cannot be updated or deleted through the introduced model surface.
8. GREEN: implement only the smallest ORM and migration changes required for each focused behavior.
9. REFACTOR: only within the allowlist and only while the focused tests remain green.

## Verification Commands

- Run the repository atomic task checker for this task file before implementation.
- Run only the focused test file and targeted lint/static checks named by the implementing runbook.
- Inspect the migration upgrade from parent `v8_d4_annuity_lineage_01`; verify exact revision IDs, the resulting SQLite schema, application UUID behavior, check/unique constraints, and all three restricted foreign keys.
- Validate baseline-subtracted scope against the exact allowlist before reporting completion.
- Do not run broad/full-repository verification except at an explicitly authorized manifest close point.

## SQLite Serialization

- This task owns a migration and SQLite-writing verification; it must execute in the repository serialization lane.
- The worker must report `READY_FOR_SERIAL_TEST` and wait for explicit controller `GRANT` before acquiring the repository lock.
- Only after the lock is acquired may migration or SQLite-writing tests start.
- Release the lock after verification and record acquisition, commands, exit status, and release in task evidence.

## Evidence

- Initialize evidence only through `./scripts/evidence_init.sh FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01 --task-file tasks/postdemo/v8/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01.md --allowlist tasks/postdemo/v8/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01.md backend/app/modules/fees/models.py backend/alembic/versions/v8_delta4_legacy_fee_reduction_provenance.py backend/tests/test_v8_legacy_fee_reduction_provenance_carrier.py artifacts/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01/**`.
- Record required commands and latest exit statuses in task-local `results.jsonl` with corresponding logs.
- Produce task-local `summary.md`, baseline-subtracted scoped `git/diff.patch`, dirty-baseline artifacts when applicable, and scope-validation output.
- Obtain one independent domain reviewer verdict with `approved`, zero findings, and task-local evidence; the implementer may not self-approve.
- Run the repository task gate and atomic evidence validation before reporting `PASS`.
- Report only `PASS`, `FAIL`, or `BLOCKED`, backed by the evidence bundle.

## Evidence Path

- `artifacts/FPMS-V8-LEGACY-FEE-REDUCTION-PROVENANCE-CARRIER-20260715-01/**`

## Remaining Follow-Up Task IDs

- None are absorbed by this task. Importer, backfill, approval, activation, and customer-decision work remains outside this closure and requires separately frozen task IDs.

## Acceptance

- Closure is limited to the single provenance carrier, its migration, and focused tests.
- All exact D4-12 fields, constraints, lineage, UUID generation, legacy-value grammar, bidirectional approval nullability, unique identity, confirmation audit, and immutability checks pass.
- No fee-schedule-item/source-record alternative, importer, backfill, approval creation, customer confirmation/decision, inferred actor/time, or outside-allowlist change exists.
- SQLite serialization, independent review, task gate, atomic evidence validation, and evidence requirements all pass.
