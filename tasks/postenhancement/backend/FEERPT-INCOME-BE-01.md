# FEERPT-INCOME-BE-01

- exact closure slice: extend `GET /fees/drafts` summary with `agent_service_amounts` using `T_CaseAgentSplit` first and `primary_agent_id` fallback for service-fee attribution
- explicit non-closure: no frontend work, no billed/received/unpaid semantics, no trend reporting, no schema changes
- remaining follow-up task ids: `FEERPT-INCOME-FE-01`, `FEERPT-INCOME-QA-01`
