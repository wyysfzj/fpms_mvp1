# CASERPT-AGG-BE-01

- exact closure slice: extend `GET /cases` summary with `country_counts` and `agent_counts` for the case-report aggregate residual slice
- explicit non-closure: no grant-rate, no trend reporting, no frontend work, no schema changes
- remaining follow-up task ids: `CASERPT-AGG-FE-01`, `CASERPT-AGG-QA-01`
