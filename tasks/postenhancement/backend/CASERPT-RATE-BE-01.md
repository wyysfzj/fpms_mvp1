# CASERPT-RATE-BE-01

- exact closure slice: extend `GET /cases` summary with `granted_count`, `grant_rate`, `terminated_count`, `invalidated_count`, and `in_prosecution_count` using the frozen grant-rate semantics
- explicit non-closure: no trend reporting, no frontend work, no schema changes
- remaining follow-up task ids: `CASERPT-RATE-FE-01`, `CASERPT-RATE-QA-01`
