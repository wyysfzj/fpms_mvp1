# PE-BE-00-04 — 新增 `GET /auth/me` 返回当前用户角色与权限（解除 FE-00-02 阻塞）

- Source: Wave 02 blocker remediation
- Type: `endpoint`
- Execution mode: Atomic (single-task, single-owner)

## Task Definition

- 目标：新增鉴权端点 `GET /auth/me`，返回当前登录用户基础信息、角色列表、权限列表。
- Allowlist:
  - `backend/app/modules/auth/api.py`
  - `backend/app/modules/auth/schemas.py`
- 依赖：无
- 验收：
  - 已登录请求返回 200，结构包含 `user` / `roles` / `permissions`
  - 未登录返回 401（沿用现有 `AUTH_REQUIRED` 语义）
  - 不改变现有 `/auth/login` 契约
- 验证：`cd backend && pytest -q`

## Execution Checklist

- [ ] Confirm allowlist only
- [ ] Implement exactly this task
- [ ] Run listed verification commands
- [ ] Generate artifacts evidence
