# FEERPT-AGG-FE-01

- exact closure slice: render `client_amounts`, `case_type_amounts`, and `country_amounts` grouped summaries on `FeeDraftList.vue` using the existing fees API client/types
- explicit non-closure: no new page, no chart/export, no agent-income or trend UI, no backend edits beyond API client/types
- remaining follow-up task ids: `FEERPT-AGG-QA-01`
