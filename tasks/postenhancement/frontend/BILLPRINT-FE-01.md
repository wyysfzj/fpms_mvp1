# BILLPRINT-FE-01

- exact closure slice: add the first-round bill print entry to `BillList.vue` by reusing the existing `printBill` FE API and existing bill detail print/download interaction semantics
- explicit non-closure: no backend changes, no preview page, no batch print, no PDF export, no email, no print history, no template changes
- remaining follow-up task ids: `BILLPRINT-QA-01`
