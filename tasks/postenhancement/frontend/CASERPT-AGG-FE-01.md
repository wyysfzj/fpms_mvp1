# CASERPT-AGG-FE-01

- exact closure slice: render `country_counts` and `agent_counts` grouped summaries on `CaseList.vue` using the existing case-report page and cases API client/types
- explicit non-closure: no new page, no chart/export, no grant-rate or trend UI, no backend edits beyond API client/types
- remaining follow-up task ids: `CASERPT-AGG-QA-01`
