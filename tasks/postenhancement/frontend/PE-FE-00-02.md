# PE-FE-00-02 — 登录后拉取真实权限并写入 auth store（避免 permissive unknown）。

- Source: `tasks/postenhancement/POSTENH_ATOMIC_FRONTEND_TASKS.md`
- Type: `service`
- Execution mode: Atomic (single-task, single-owner)

## Task Definition

- 目标：登录后拉取真实权限并写入 auth store（避免 permissive unknown）。
- Allowlist:
  - `frontend/src/stores/auth.ts`
  - `frontend/src/api/system.ts` 或新增 `frontend/src/api/auth.ts`
- 依赖：PE-FE-00-01
- 验收：`hasAnyPermission` 在权限已加载后可准确控制 UI。
- 验证：`cd frontend && npm run lint && npm run typecheck`

## Execution Checklist

- [ ] Confirm allowlist only
- [ ] Implement exactly this task
- [ ] Run listed verification commands
- [ ] Record manual verification if UI task
