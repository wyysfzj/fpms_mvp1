# CASERPT-RATE-FE-01

- exact closure slice: render the grant-rate-related summary metrics on `CaseList.vue` using the existing cases API client/types
- explicit non-closure: no new page, no chart/export, no trend UI, no backend edits beyond API client/types
- remaining follow-up task ids: `CASERPT-RATE-QA-01`
