# Batch A1 Review Report

> Generated by: review-agent
> Date: 2026-02-24
> Updated: 2026-02-24 (post-fix pass)
> Verdict: **APPROVED**

## Summary

**Overall Assessment: PASS.** All 4 WARNING-level issues from the initial review have been fixed. Batch A1 now delivers a complete, well-tested implementation of TaskTemplate CRUD, TaskLog API, and auto-generation with full audit trail. 42/42 tests pass.

## Files Reviewed

| # | File | Status | Notes |
|---|------|--------|-------|
| 1 | `alembic/versions/a1_task_template_fields.py` | PASS | Idempotent migration with column-existence checks, `batch_alter_table` for SQLite, correct chain (down_revision = `53f7a0c139cc`) |
| 2 | `modules/tasks/models.py` | PASS | TaskTemplate, Task, TaskLog all use UUIDPrimaryKeyMixin + AuditMixin + Base. `server_default=text("1")` for Boolean is SQLite-safe. UUIDs are String(36). |
| 3 | `modules/tasks/enums.py` | PASS | Clean str Enum definitions. AUTO_CREATE and AUTO_CREATE_FROM_DOCUMENT added correctly. |
| 4 | `modules/tasks/schemas.py` | PASS | TaskTemplateCreateIn/UpdateIn/Out + TaskLogOut follow Pydantic v2 patterns. ConfigDict(from_attributes=True) correct. Field validations present (min_length, max_length). |
| 5 | `modules/tasks/service.py` | PASS | Template CRUD with duplicate-code 409 check, list_task_logs with task existence validation. `create_task()` now writes CREATE log. `model_dump(exclude_unset=True)` for partial updates is correct. |
| 6 | `modules/tasks/api.py` | PASS | All new endpoints have `require_perm()` as Depends parameter. Route ordering correct — `/tasks/{task_id}/logs` before `/tasks/{task_id}`. 201 status for POST create. |
| 7 | `modules/tasks/task_generation_service.py` | PASS | Removed `_is_office_action()` gate — any doc_type matching a template code triggers auto-creation. `_compute_due_date()` handles both `add_days` and `add_months` with day-clamping. `_is_incoming()` cleaned up (dead `flow_dir` fallback removed). |
| 8 | `modules/rbac/service.py` | PASS | `TaskTemplate.Create`, `TaskTemplate.Edit`, `TaskTemplate.Read` added to Admin role. Seed function is idempotent. |
| 9 | `scripts/seed_dev.py` | PASS | `seed_task_templates()` is idempotent (checks by code). OA_REPLY (add_days=120, inner_offset_days=14) and GRANT_FEE (add_days=60, inner_offset_days=7) seeded. Called in `main()`. |
| 10 | `tests/test_task_template.py` | PASS | 9 tests covering: seeded template verification, create with add_days=90, update, duplicate code 409, task logs with CREATE entry, auto-generation due_date, GRANT_FEE trigger, and add_months calculation. |
| 11 | `tests/conftest.py` | PASS | `seed_data` fixture now calls `_seed_task_templates()` to seed OA_REPLY and GRANT_FEE into test DB. |

## Acceptance Criteria Checklist

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | GET /api/v1/task-templates returns seeded templates | **PASS** | `test_list_task_templates_returns_seeded` asserts OA_REPLY (add_days=120, inner_offset_days=14) and GRANT_FEE (add_days=60, inner_offset_days=7) present. |
| 2 | POST /api/v1/task-templates creates with add_days=90 | **PASS** | `test_create_task_template` creates with add_days=90 and asserts body["add_days"] == 90. |
| 3 | PUT /api/v1/task-templates/{id} updates fields | **PASS** | `test_update_task_template` verifies name and add_days update. |
| 4 | GET /api/v1/tasks/{id}/logs returns audit entries | **PASS** | `test_list_task_logs_includes_create` verifies both CREATE and CLOSE logs appear. |
| 5 | Document direction=IN auto-creates task with correct due_date | **PASS** | `test_task_generation_creates_task_with_due_date` asserts expected_due = date(2025,1,10) + timedelta(days=120). |
| 6 | Auto-created task has internal_due_date = due_date - inner_offset_days | **PASS** | Test asserts expected_internal = expected_due - timedelta(days=14). |
| 7 | Duplicate template code → 409 error | **PASS** | `test_duplicate_code_rejected` verifies r2.status_code == 409. |
| 8 | Quality gate passes | **PASS** | ruff clean, 42/42 pytest, alembic + seed + healthz all verified. |

## Issues Found (Initial Review) — All Resolved

### Warning-level (all 4 FIXED)

| Severity | Description | Resolution |
|----------|-------------|------------|
| ~~WARNING~~ FIXED | `_is_office_action()` blocked GRANT_FEE from auto-triggering | Removed `_is_office_action()` entirely. Template lookup by `code == doc_type` is now the sole filter. Any template whose code matches a document's doc_type will trigger. Test: `test_task_generation_grant_fee_triggers`. |
| ~~WARNING~~ FIXED | `add_months` field defined but never used | Added `_compute_due_date()` and `_add_months()` helper. Months applied first (with day-clamping, e.g. Jan 31 + 3mo = Apr 30), then days added. Test: `test_task_generation_add_months`. |
| ~~WARNING~~ FIXED | Manual `create_task()` had no CREATE log | Added `_create_task_log(action=TaskAction.CREATE)` in `service.py:create_task()`. Test: `test_list_task_logs_includes_create` asserts CREATE log present immediately after task creation. |
| ~~WARNING~~ FIXED | Test conftest didn't seed templates; seeded data not verified | Added `_seed_task_templates()` to conftest `seed_data` fixture. Test `test_list_task_templates_returns_seeded` now verifies OA_REPLY and GRANT_FEE with exact field values. |

### Info-level (unchanged, low priority)

| Severity | Description | File |
|----------|-------------|------|
| INFO | TaskTemplate/Task/TaskLog class names lack `T_` prefix convention. Consistent with existing Case model, not a deviation. | models.py |
| INFO | task_generation_service.py uses `db.query()` (legacy) instead of `select()` (2.0 style). Inconsistent with service.py. | task_generation_service.py |
| INFO | `downgrade()` is empty (pass). Consistent with project's forward-only migration policy. | a1_task_template_fields.py |

## Recommendations (remaining)

1. **Align query style**: Refactor task_generation_service.py to SQLAlchemy 2.0 `select()` style for consistency.

## Verdict

### APPROVED

All 8 acceptance criteria pass. All 4 WARNING-level issues from the initial review have been fixed and verified with new tests (42/42 pass). The implementation is correct, secure, SQLite-compatible, and has full audit trail coverage.
