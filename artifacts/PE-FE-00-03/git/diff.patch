diff --git a/docs/FPMS_Frontend_Manual_Test_User_Guide.md b/docs/FPMS_Frontend_Manual_Test_User_Guide.md
new file mode 100644
index 0000000..3f046cb
--- /dev/null
+++ b/docs/FPMS_Frontend_Manual_Test_User_Guide.md
@@ -0,0 +1,342 @@
+# FPMS MVP1 Frontend — 手工测试 User Guide（BE 已启动）
+
+本指南用于你在完成 **NEXT‑1/2/3** 后，对前端做一次**可复现、可记录证据**的手工测试（从启动前端开始）。
+
+**已知信息（来自你们 runtime smoke 证据）：**
+- Backend Base URL：`http://localhost:8000/api/v1`
+- 登录接口：`POST /api/v1/auth/login`（JSON）
+- 登录返回 token 字段：`access_token`
+- 测试账号：`admin / admin123`  
+这些信息在你们的 FE‑3 Runtime Smoke Evidence 中已被验证。 fileciteturn0file0
+
+---
+
+## 0. 测试准备（强烈建议）
+
+### 0.0 DEMO-UI 模式说明
+
+当环境变量 `VITE_DEMO_UI=1` 时，前端启用 Demo 层：
+- **Style-B 主题**：`body` 添加 `style-b` 类，sidebar 激活态/悬浮态使用 CSS 变量覆盖
+- **全站中文化**：所有按钮、表格列头、对话框、空状态文本均为中文（源自 `labels.zh.ts`）
+- **分组菜单**：侧边栏按 工作台/案件管理/期限监控/客户中心/系统设置 分组
+- **Dashboard KPI**：工作台显示4个统计卡片 + 待办任务表格
+
+本指南中的英文按钮名称（如 "New Case"、"Edit Case"）在 Demo 模式下对应中文标签（如 "新建案件"、"编辑案件"）。完整映射见 `docs/frontend_smoke_flows.md` Section 9。
+
+如需测试 Demo 层，请确保 `.env` 中设置 `VITE_DEMO_UI=1` 后再启动前端。
+
+### 0.1 建议准备的“证据采集工具”
+- 浏览器 DevTools（Network + Console）
+- 终端（用于保存 curl 输出 / 启动日志）
+- 截图工具（关键页面与关键错误提示）
+
+### 0.2 建议的“证据记录模板”（复制到你自己的测试记录里）
+- 测试时间：
+- 前端 commit / tag：
+- 后端 commit / tag：
+- 前端启动命令与端口：
+- 后端启动命令与端口（你已启动）：
+- 测试账号：
+- 结论：PASS / PARTIAL / FAIL
+- 失败项清单（每条包含：页面/步骤/期望/实际/requestId/截图）
+
+---
+
+## 1. 后端连通性确认（你已启动，但建议再确认一次）
+
+在终端执行（不带 token 预期返回 401，而不是 000）：
+
+```bash
+curl -i "http://localhost:8000/api/v1/clients?page=1&page_size=1"
+```
+
+**预期：**
+- HTTP 401，且 body 是标准 error envelope（`{"error":{"code","message","details"}}}`）
+- header 通常包含 `x-request-id`（用于定位问题）
+你们之前 smoke 里也观察到了这种返回。 fileciteturn0file0
+
+如果这里不是 401/403/200，而是 `000`，说明端口/服务仍不通——此时不要继续测前端。
+
+---
+
+## 2. 手工启动前端（Vite）
+
+### 2.1 安装依赖（首次或依赖变更后）
+```bash
+cd frontend
+npm install
+```
+
+### 2.2 启动开发服务器
+```bash
+npm run dev
+```
+
+**预期：**
+- 终端输出类似：
+  - `VITE vX.Y.Z  ready in ...`
+  - `Local: http://localhost:5173/`（端口以实际输出为准）
+- 浏览器访问该 Local URL 能加载登录页/或自动重定向到登录页。
+
+### 2.3 确认前端指向正确的后端 Base URL
+在前端界面打开后，建议在 DevTools Console 中查看你们应用是否打印了 API baseURL（如果你们做了 About/Debug 页面也可从那里确认）。
+若无 UI 提示，则用 Network 面板确认请求是否发往：
+- `http://localhost:8000/api/v1/...`
+
+---
+
+## 3. 登录与会话（Auth / Session）
+
+### 3.1 登录
+1) 打开前端页面（Vite Local URL）  
+2) 输入：
+   - username：`admin`
+   - password：`admin123`
+3) 点击 Login/Sign in
+
+**预期：**
+- 调用 `POST /api/v1/auth/login`，返回 200，响应含 `access_token`。 fileciteturn0file0
+- 登录成功后进入 App Shell（侧边栏 + Header + 内容区）
+
+### 3.2 token 持久化验证（localStorage）
+1) 登录成功后，打开 DevTools → Application → Local Storage  
+2) 找到 token（key 名以实现为准）  
+3) 刷新页面（F5），应保持登录态，不应回到登录页。
+
+### 3.3 401 路由守卫验证
+1) 在 Local Storage 中删除 token  
+2) 访问一个受保护页面（例如 `/clients`）  
+**预期：**
+- 自动跳转到登录页（或提示未登录）
+
+---
+
+## 4. 前端可复用错误码与状态码处理规范（对齐后端 Envelope）
+
+> 目的：把错误处理沉淀为可复用规则，供所有页面/弹窗/表单统一使用。
+
+### 4.1 后端错误 Envelope 与 requestId 读取规则
+后端错误响应统一按以下结构处理：
+
+```json
+{
+  "error": {
+    "code": "SOME_CODE",
+    "message": "Human readable message",
+    "details": {}
+  }
+}
+```
+
+前端统一规则：
+- 从响应 header 读取 `x-request-id`（大小写不敏感，Axios 中通常为小写键）。
+- UI 错误文案统一追加 requestId：`{message}（请求ID：{requestId}）`；若无 requestId，仅显示 message。
+- 日志/测试记录最少保留：`path + method + status + error.code + requestId`。
+- 对 403 若 `details.required_perm` 存在，页面应可显示“所需权限”。
+
+### 4.2 状态码处理矩阵（400/401/403/404/409/422）
+| HTTP | 后端语义 | 前端处理（可复用） |
+|---|---|---|
+| 400 | 业务校验失败（BusinessError） | 显示业务错误 banner/toast；保留当前表单输入；展示 requestId。 |
+| 401 | 未认证/登录失效 | 清理本地 token；跳转登录页；提示“会话已过期，请重新登录”。 |
+| 403 | 无权限 | 跳转或展示 Permission Denied；展示 `required_perm`（若有）与 requestId。 |
+| 404 | 资源不存在 | 列表页提示“数据不存在或已删除”；详情页跳转 NotFound 或返回列表。 |
+| 409 | 冲突/重复/配置缺失 | 用“冲突/已存在/当前状态不允许”语义提示；引导用户修正输入后重试。 |
+| 422 | 请求参数校验失败 | `details.errors`/字段错误映射到表单项；同时显示页面级错误摘要与 requestId。 |
+
+### 4.3 页面层统一落地流程（建议）
+1) API 调用失败后，先走统一错误归一化（`status/code/message/details/requestId`）。  
+2) 按 4.2 矩阵分支处理；禁止每个页面自定义“另一套”错误语义。  
+3) 表单页优先展示字段错误（422），再展示全局 banner（400/409 等）。  
+4) 任何可见错误提示都应支持携带 requestId，便于后端日志定位。  
+
+### 4.4 全局错误 UX 验证用例（含 requestId）
+#### 4.4.1 400 Business Validation
+- 在任意业务动作中构造业务校验失败（如状态不允许的操作）。
+**预期：**
+- 显示业务错误 message；
+- 保留当前页面上下文；
+- 错误提示包含 requestId（若后端返回）。
+
+#### 4.4.2 401 Unauthorized
+- 删除 Local Storage token 后直接访问受保护页面（如 `/clients`）。
+**预期：**
+- 自动跳转登录页；
+- 已失效会话不应继续调用受保护接口；
+- 如显示错误提示，应包含 requestId（若有）。
+
+#### 4.4.3 403 Permission Denied
+- 用低权限账号访问受限动作。
+**预期：**
+- 显示 Permission denied 页面/卡片；
+- 如后端返回 `required_perm`，页面可见；
+- requestId 可见。
+
+#### 4.4.4 404 Not Found
+- 手工访问不存在资源详情（如 `/cases/<不存在ID>`）。
+**预期：**
+- 显示“资源不存在/已删除”语义；
+- 页面可恢复（返回列表或 NotFound）；
+- requestId 可见（若有）。
+
+#### 4.4.5 409 Conflict
+- Cases：重复 `case_no` 提交（你们 runtime smoke 中出现过 duplicate case_no 的 409） fileciteturn0file1
+**预期：**
+- 冲突语义提示明确；
+- 不出现空白页；
+- requestId 可见（若有）。
+
+#### 4.4.6 422 Validation Error（表单字段错误）
+在任一创建表单故意漏填必填字段提交：
+- Clients：必填字段（如 name_cn 映射后的字段）
+- Tasks：`due_date` 必填（你们 runtime 合同修复里明确了 due_date 是必填） fileciteturn0file1
+**预期：**
+- 字段级错误显示在对应输入项附近；
+- 非字段错误显示在页面顶部 banner；
+- requestId 可见（若后端返回）。
+
+---
+
+## 5. 业务链路手工 Smoke（按模块顺序）
+
+下列步骤覆盖 MVP1 核心模块。建议你边测边在 Network 面板记录：
+- URL + method
+- status
+- requestId（若有）
+- 错误 envelope（若失败）
+
+### 5.1 Clients（客户）
+**目标：** list → create → edit → deactivate
+1) 进入 Clients 列表（侧边栏点击或直接路由 `/clients`）
+   - 预期：列表加载成功；分页可用
+2) New Client
+   - 填写必要字段提交
+   - 预期：201/200，回到列表或显示成功提示
+3) Edit Client
+   - 修改并保存
+4) Deactivate Client
+   - 预期：状态变化；列表可反映
+
+### 5.2 Cases（案件）
+**目标：** list → create(关联 client) → detail → edit → limited-edit
+1) 进入 Cases 列表 `/cases`
+2) Create Case
+   - 选择/填写 client_id（或通过 UI 选择客户）
+   - case_no 需要唯一（避免 409）
+3) 进入 Case detail `/cases/:id`
+4) Edit / Limited-edit
+   - 提交成功后 detail 刷新
+
+**Focus/Immersive Mode（如你们实现了切换按钮）：**
+- 在 Case detail 的长文本 tab（例如 Claims）切换沉浸模式
+- 预期：布局变为单栏阅读流，侧栏/时间线隐藏，排版更适合阅读（参考 `reference/case_detail.html` 的行为范式）
+
+### 5.3 Tasks（任务）
+**目标：** list → create(case_id + due_date) → close/reopen/cancel → today
+1) `/tasks` 列表加载
+2) Create Task `/tasks/new`
+   - 必填：`case_id`、`due_date`
+3) 在列表行操作中执行 close/reopen/cancel
+4) `/tasks/today`
+   - 切换 worker / supervisor（如 UI 存在）
+
+### 5.4 Documents（文书）
+**目标：** list → create → detail → upload/download attachments
+1) `/documents` 列表
+2) Create Document `/documents/new`
+   - 注意：你们之前合同修复指出 `doc_date`、`doc_template_id` 等字段存在“key 必须出现”的要求（doc_template_id 可为 null 但 key 需要存在） fileciteturn0file1
+3) 进入 detail `/documents/:id`
+4) 上传附件
+   - NEXT‑1 完成后：预期 upload 返回 2xx，不再 500；失败也应显示 requestId
+5) 下载附件
+   - 预期：浏览器触发下载（blob）
+
+### 5.5 Fees（费用）
+**目标：** rates → drafts → items → lock/unlock
+1) Fee rates：创建/编辑费率（fee_code/fee_type 等应按你们修复后的字段）
+2) Fee drafts：创建 draft
+3) Draft detail：添加 items、编辑 items、删除 items
+4) lock/unlock：锁定后 items 应不可编辑
+
+### 5.6 Billing（账单）
+**目标：** bills → payments → print → offsets → receipts (如支持)
+1) Bills list `/billing/bills`
+2) Create Bill（从 drafts 或 manual）
+3) Print Bill
+   - NEXT‑2 完成后：预期不再 409（template not configured），应返回 200 blob 并下载/预览
+4) Payments
+   - 创建 payment（注意 client_id 等字段应由 UI/适配层保证）
+5) Offsets
+   - NEXT‑3 完成后：offset create 需要有效 `payment_line_id`；应能在 UI 中选择并创建成功
+6) Case receipts summary
+   - 若你们后端/数据支持，应能显示 summary；否则记录 404/empty 的原因
+
+### 5.7 System / Templates（系统配置）
+**目标：** templates/params/letterheads
+1) Templates list/create（注意：你们 runtime 证据指出模板上传在后端是 metadata-only（file_path）语义，而不是 multipart 上传语义） fileciteturn0file1
+2) Params list/upsert
+3) Letterheads list/create
+
+### 5.8 DEMO-UI 层验证（VITE_DEMO_UI=1 时执行）
+
+> 此节验证 DEMO-UI FIX-01/02/03 的修复效果。需在 `VITE_DEMO_UI=1` 模式下运行前端。
+
+#### 5.8.1 Style-B 主题 — 侧边栏激活态（FIX-01）
+1) 打开 DevTools → Elements，确认 `body` 有 `style-b` class
+2) 点击任一侧边栏菜单项
+3) 检查激活项 `.nav-item.router-link-active` 的 computed style：
+   - `background-color` 应为 `#EFF6FF`
+   - `color` 应为 `#2563EB`
+4) 悬浮在另一个非激活菜单项上：
+   - `background-color` 应为 `#F1F5F9`
+
+#### 5.8.2 列表页中文标签（FIX-03）
+遍历以下列表页，确认所有 UI 标签为中文：
+- `/cases` — 标题「案件列表」，列头「编号/案号/标题/客户/状态/更新时间/操作」，按钮「新建案件」
+- `/tasks` — 标题「任务列表」，列头含「优先级/截止日期/负责人」
+- `/billing/bills` — 标题「账单列表」，列头含「账单号/金额/余额/开票日」
+- `/fees/drafts` — 标题「费用草稿」，列头含「草稿编号/币种」
+- `/documents` — 标题「文档列表」，列头含「方向/类型/日期」
+
+#### 5.8.3 详情页中文标签（FIX-02）
+进入以下详情页，确认所有 UI 标签为中文：
+- `/cases/{id}` — 按钮「返回/编辑案件」，tab「概览/权利要求/官方文件/费用/账单/任务」，字段标签「案件信息/案号/标题/客户/状态/申请日/优先权日/备注」
+- `/documents/{id}` — 按钮「返回/编辑文档」，标签「文档内容/文档信息/编号/方向/创建时间/更新时间」
+- `/fees/drafts/{id}` — 按钮「返回/刷新/锁定/解锁」，tab「明细/概览」，标签「草稿信息/草稿编号/状态/案件编号/客户编号/类型/币种」
+- `/billing/bills/{id}` — 按钮「返回/打印账单/刷新」，tab「明细/概览」，列头「描述/数量/单价/金额」，标签「账单信息/账单号/客户/案件/币种/开票日/到期日」
+
+#### 5.8.4 确认对话框中文（FIX-02/03）
+- 任务列表点击「操作 → 关闭」 — 弹窗标题「关闭任务」，确认按钮「关闭」
+- 费用草稿详情点击「🔒 锁定」 — 弹窗标题「锁定草稿」，确认按钮「锁定」
+
+#### 5.8.5 无英文泄漏检查
+在 Demo 模式下完整浏览所有页面，确认：
+- 无英文按钮标签、表头、空状态文本
+- 例外：数据值（状态码 `OPEN`/`LOCKED`、币种 `CNY` 等）保持英文大写，属于数据而非 UI 标签
+
+---
+
+## 6. 手工测试“快速回归”清单（10–15 分钟）
+当你只想快速确认“没有回归”，跑下面 8 条即可：
+
+1) 前端 `npm run dev` 能启动并打开页面  
+2) admin 登录成功（拿到 token） fileciteturn0file0  
+3) Clients 列表加载 + 新建客户成功  
+4) Cases 新建 + 进入 detail  
+5) Tasks 新建（case_id + due_date）成功  
+6) Documents 新建 + 上传附件成功（NEXT‑1 验证点）  
+7) Bill print 成功（NEXT‑2 验证点）  
+8) Offset 创建成功（NEXT‑3 验证点）
+
+---
+
+## 7. 出现问题时如何记录（最小可修复证据）
+对任何失败步骤，请至少记录：
+- 页面路由（例如 `/documents/xxx`）
+- 触发动作（按钮名）
+- Network：URL + status + response body
+- `x-request-id`（如果有）
+- 后端 uvicorn 控制台报错栈（如果是 500）
+- 截图（错误提示 + 控制台/网络详情）
+
+这样你再交给 Codex/Claude/Gemini 修复时，就能完全 evidence-driven，避免猜测。
